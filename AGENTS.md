# 개발 작업 부하

이 저장소와 연결된 워크트리에서는 CPU를 많이 쓰는 검증을 동시에 여러 개 돌리지 않는다.
전체 테스트·빌드·물리 시뮬레이션은 순서대로 실행하고, Vitest 기본 워커 제한을 유지한다.
시뮬레이션은 질문에 필요한 후보/시드만 실행한다. 관련 검증이 통과한 뒤 새 변경이나 실패 없이 반복 실행하지 않는다.
검사 범위를 줄여 실패를 숨기지 말고 변경에 필요한 검증은 완료한다.
검증용 브라우저와 임시 서버는 종료 시 정리한다. 사용자가 실행한 다른 프로세스는 중지하지 않는다.
측정 방법과 한계는 docs/DEVELOPMENT_PERFORMANCE.md 참고.
