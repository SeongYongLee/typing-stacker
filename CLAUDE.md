# Typing Stacker

한글 타자게임 + 물리 쌓기. 싱글과 대전(최대 8명) 둘 다 돌아가고 배포되어 있다.

## 작업 문서 운영 (필수)

이 프로젝트는 **Obsidian 볼트를 기준 문서로 함께 운영한다.** 이 파일에는 구현 관점(밟은 함정, 제약)이 남고, 볼트에는 기획 근거와 작업 이력이 남는다.

- 진입점: `~/Library/Mobile Documents/com~apple~CloudDocs/Obsidian/Obsidian/Projects/Typing Stacker/00_Index.md`
- URI: `obsidian://open?vault=Obsidian&file=Projects%2FTyping%20Stacker%2F00_Index`

작업을 재개할 때 그 진입점을 먼저 읽고, **의미 있는 변화가 생기면 해당 노트를 갱신한다.**

| 바뀐 것 | 갱신할 노트 |
|---|---|
| 규칙·기획 결정 | `01_Planning` |
| 커밋 / 잡은 버그 / 밸런스 측정 | `02_Work_History` |
| 구조·의존성 경계·파이프라인 | `03_Architecture` |
| 새 과제·리스크 / 백로그 해소 | `04_Backlog` |
| 현재 상태 요약, `Last updated` | `00_Index` + 볼트 루트 `Typing Stacker.md` |

노트에는 코드가 이미 말해주는 것을 옮겨 적지 않는다. **결정의 이유, 버린 방법, 측정한 숫자**를 남긴다. 특히 밸런스 측정값은 표로 누적해 "어디까지 시도해봤는지"가 보이게 한다.

## 기술 스택

- React 19 + TypeScript 6 (strict, noUncheckedIndexedAccess) + Vite 8
- 물리: `@dimforge/rapier2d-compat` (WASM 인라인)
- 렌더: 아레나만 Canvas 2D. 낙하 단어·입력창·HUD는 DOM
- 테스트: Vitest (node env)
- 린터: oxlint (Vite 템플릿 기본)
- pnpm

## 게임 스펙

| 항목 | 결정 |
|---|---|
| 낙하 글자 | 한글 단어 (IME 조립 중 글자까지 표시) |
| 드롭 트리거 | Enter **한 번**. 그 순간의 화살표 x로 즉시 낙하 |
| 물건 공개 | Enter 이후. 치기 전에는 무엇이 나올지 모른다 |
| 합치기 | 재료가 서로 닿으면 합쳐진다. 레시피 44개(재료 둘 27 · 셋 이상 17) |
| 국면 | 첫 밤(합성 **두 번**까지, 상한 35초) → 낮 20초 ↔ 밤 10초. 밤에는 재료만 내려온다 |
| 목숨 | 3개. 물건이 받침대를 벗어날 때마다 하나 줄고 0이면 종료 |
| 히든 | 낮은 확률로 모양이 다른 변형. 유리할 수도 불리할 수도 있다 |
| 물건 | 단어 107개 · 히든 38 · 합성 전용 40 = **185종**. 히든을 가진 단어는 107개 중 37개 |
| 콤보 | 단어를 맞출 때마다 +1. **놓치거나 목숨을 잃으면** 초기화 (오타로는 안 끊김) |
| 플랫폼 | 데스크톱 가로. 물리 키보드 전용. **가로 1200px 미만이면 판을 열지 않고 안내만 띄운다** |

## 아키텍처 원칙

1. **`game/systems/`와 `game/data/`는 DOM·canvas·Rapier 의존성이 0이다.** node 환경에서 그대로 테스트가 돌아야 한다. 돌지 않으면 경계가 깨진 것이다.
2. **`Math.random()` 직접 호출 금지.** `Rng` 인터페이스(mulberry32)를 주입한다. 같은 시드면 단어 순서와 히든 결과가 재현된다.
3. **시간은 루프가 주입하는 delta로만 흐른다.** `Date.now()`는 시드를 뽑는 경계(`useGameEngine`)에서만 쓴다.
4. **React는 껍데기.** `GameEngine`이 상태를 소유하고 프레임마다 `onStateChange`로 스냅샷을 밀어준다.
5. **엔진은 소리를 모른다.** 상태와 **사건**(`onEvent`)을 따로 내놓고, 그것을 소리로 바꾸는 것은 `audio/`의 일이다. 렌더러와 같은 관계다.

1~3은 나중에 서버가 같은 로직을 돌려 검증할 수 있게 하려는 것이다. 멀티 자체는 선행 구현하지 않았다.

### 상태와 사건을 나눈 이유

`onStateChange`는 **지금 어떤가**를, `onEvent`는 **무슨 일이 일어났는가**를 말한다. 소리는 뒤쪽이어야 한다. 상태 스냅샷을 프레임마다 비교해 소리를 내려 하면 한 프레임에 두 번 일어난 일이 하나로 뭉개지고(무너질 때 부딪힘이 여럿이다), 값이 같은 자리로 되돌아온 것과 아무 일도 없던 것을 구분할 수 없다.

## 좌표계

아레나는 미터 단위로 시뮬레이션한다(Rapier는 수 미터 규모에서 가장 안정적이다). 원점은 받침대 중앙, x는 오른쪽 +, y는 **위쪽 +**. 캔버스는 y가 아래로 +이므로 렌더러가 뒤집는다 — 회전도 부호를 뒤집어야 한다.

## 이미 밟은 함정

지나간 자리에 다시 빠지지 않도록 남긴다.

### 한글 IME + Enter (실기로만 발견됨)

- 조립 중 Enter에 `if (e.nativeEvent.isComposing) return` 가드를 넣으면 **Enter를 두 번** 눌러야 제출된다. 이 게임에서 Enter 타이밍은 곧 조준이라 치명적이다. 그래서 가드를 두지 않고 `isComposing`이어도 Enter를 받아들이고 그 시점의 DOM `value`를 제출값으로 쓴다.
- macOS Chrome은 조립 확정 Enter에 대해 keydown을 **두 번** 보낸다(`isComposing` true → false). 두 번째 Enter가 방금 비운 입력창을 제출해 성공 피드백을 "(빈 입력) ✗"로 덮어쓰므로, 빈 입력은 제출하지 않는다.
- 제출 직후 조립 확정 이벤트가 값을 되살려 놓는다. `swallow` 플래그로 삼키되, 플래그는 `compositionend`가 아니라 **다음 keydown**에서 푼다 — `input`과 `compositionend`의 발생 순서가 브라우저마다 달라서 순서에 의존하지 않는 기준점이 keydown뿐이다.
- headless 브라우저로는 IME를 재현할 수 없다. 이 영역은 반드시 실기로 확인해야 한다.

### Rapier

- **모듈을 동적으로 받는다.** `PhysicsWorld` 안에서 `await import('@dimforge/rapier2d-compat')`로 가져온다. 정적 import로 두면 base64 WASM 한 덩이(1.6MB)가 메인 번들에 들어가고, 브라우저는 **그것을 다 받아 파싱할 때까지 화면에 아무것도 그리지 못한다.** 타입은 `import type`으로 따로 가져온다 — 값으로 쓰는 순간(`ColliderDesc.ball(...)`) 번들에 다시 실리므로, 값은 전부 `rapier().X` 를 거친다.
- 앞당겨지는 것은 **첫 그림**이지 시작 가능 시점이 아니다. 타이틀의 "혼자 하기"는 여전히 물리가 준비될 때까지 눌리지 않는다.
- `init()`은 **프로세스에 한 번만.** React StrictMode가 이펙트를 두 번 돌려 `init()`이 겹쳐 호출되면 WASM 모듈이 두 번 인스턴스화되고, 먼저 만든 World가 낡은 인스턴스를 붙들어 `recursive use of an object ...` 로 터진다. `PhysicsWorld`가 프로미스를 메모이즈한다.
- 이 빌드에는 `convexDecomposition`이 없다. `polyline`은 얇은 경계라 쌓기에 못 쓴다. 그래서 오목한 도형은 **파이프라인에서 미리 볼록 조각으로 분해**해 compound 콜라이더로 넣는다.
- 바늘처럼 얇은 폴리곤을 넘기면 퇴화 도형으로 보고 터진다. 바운딩 박스만 검사하면 대각선으로 누운 얇은 삼각형을 놓치므로 **실제 두께**(면적 ÷ 최장 대각선)를 재야 한다.
- 회전 감쇠가 약하면 실루엣이 둥근 물건(펼친 우산)이 바퀴처럼 굴러 받침대를 벗어난다. 지금 `ANGULAR_DAMPING = 2.4`.

### 밸런스

- 조준 범위는 `받침대 반폭 − 가장 큰 물건의 반폭`이다. 어느 타이밍에 Enter를 쳐도 물건이 받침대에 온전히 얹힌다 — 조준 실수 하나로 즉사하면 안 되고, 무너짐은 쌓기 실패에서만 나와야 한다. `tests/shapes.test.ts`가 이 관계를 지킨다.
- 받침대가 좁으면 한 칸 폭 탑을 쌓다가 살짝 미끄러지는 것만으로 즉사해서 실력과 무관하게 2~3개에서 끝난다. 벽이 없으니 미끄러진 물건을 받아줄 것은 받침대 폭뿐이다.
- **이탈한 물건은 반드시 세계에서 치워야 한다.** 남겨두면 계속 떨어지면서 매 프레임 이탈로 잡혀 목숨 3개가 한순간에 날아간다.
- 중앙 조준 봇으로 잰 현재 밸런스: 15개 / 2.0m / 최고 콤보 x14. 목숨 3개 도입 전에는 2~3개였다.
- **국면을 사건으로 끊고 다시 쟀다 (봇 60판 · 2026-08-09).** 첫 밤이 시간(15초)이 아니라 **합성 두 번**으로 끝나고, 낮 30 → 20초 · 밤 15 → 10초.

  | | 전 | **후** |
  |---|---|---|
  | 판 길이 (중앙값) | 37초 | **58초** |
  | 첫 밤이 끝난 시각 | 15초 고정 | **20.3초** (판마다 다르다) |
  | **밤을 본 판** | **28%** | **77%** |
  | 판당 밤에 머문 시간 | 2.8초 | **11.9초** |
  | 첫 합성에 닿은 판 | 73% | **98%** |
  | 판당 히든 | 1.58 | 2.58 |
  | 판당 합성 | 0.83 | **2.10** |

  **판당 값이 오른 것을 밀도가 올랐다고 읽으면 안 된다.** 판이 37 → 58초로 길어져서 드롭이 14.5 → 23.7개가 됐다. 드롭당으로 보면 히든은 10.9%로 **똑같고**, 합성만 5.7% → 8.9%로 올랐다 — 밤이 실제로 열린 몫이다.

  **판이 길어진 것은 부작용이 아니라 결과다.** 첫 밤이 20초로 늘어 그동안 밭이 둘이라 안정적으로 쌓이고, 합성이 늘면 통나무가 늘어 얹을 자리가 넓어진다.

- **한 판의 실측 (봇 60판 · 2026-08-09).** 물건 185종·단어 107개·레시피 44개 상태. `tests/zz-balance.measure.test.ts`로 다시 잰다.

  | | 2026-08-08 | **지금** |
  |---|---|---|
  | 판 길이 | 34.9초 | 36.6초 |
  | 떨군 횟수 | 17.9 | 12.5 |
  | 쌓인 개수 | 13.6 | 9.7 |
  | 히든 | 판당 0.75개 · 못 본 판 50% | **0.97개 · 43%** |
  | 합성 | 판당 0.27회 · 못 한 판 77% | **0.85회 · 28%** |
  | 특별한 것 합계 | 판당 1.02개 · 못 본 판 40% | **1.82개 · 13%** |

  **"아무것도 못 본 판"이 40% → 13%가 됐다.** 백로그의 "특별한 물건을 만나는 빈도"가 여기서 사실상 해소됐다 — 재작화가 히든 보유 단어를 14% → 35%로 올렸고, 첫 밤 밭과 밤 구간이 합성을 세 배로 끌어올렸다.

  **두 측정의 봇이 다르다.** 예전 것은 중앙 조준이고 이번 것은 화살표를 그대로 쓴다 — 사람이 실제로 겪는 값을 재려는 것이다. 그래서 판이 짧고(떨군 횟수 17.9 → 12.5) 덜 쌓인다. **쌓기 자체의 난이도를 비교하려면 이 두 줄을 쓰면 안 된다** — 그쪽은 `zz-bounce.measure.test.ts`의 중앙 조준 봇이 잰다.

  드롭당으로 보면 히든이 **4.2% → 7.8%**다. 판이 짧아졌는데도 판당 값이 올랐다는 뜻이다.

- **`OPENING_HIDDEN_CHANCE`를 0.05 → 0.12로 올렸다 (2026-08-09).** 0.05는 "첫 밤 밭은 히든 밀도가 **일곱 배**"라는 계산에서 나온 값인데, 평소가 14%에서 35%가 되며 배율이 **2.89배**로 내려갔다. 그 결과 첫 밤이 **평소보다도 히든이 마른 구간**이 돼 있었다 — 눈에 보이는 히든 비율이 낮 12%인데 첫 밤이 5%였다. 누르려던 것이 지나쳤던 것이다.

  | `OPENING_HIDDEN_CHANCE` | 판당 히든 | 못 본 판 | 판당 합성 | 특별한 것 합계 | 아무것도 못 본 판 |
  |---|---|---|---|---|---|
  | 0.05 (예전) | 0.97 | 43% | 0.85 | 1.82 | 13% |
  | **0.12 (지금)** | **1.27** | **30%** | 0.75 | **2.02** | **7%** |
  | 0.20 | 1.97 | 13% | — | 2.67 | 3% |

  **합성이 0.85 → 0.75로 내려간 것은 부작용이 아니라 구조다.** 첫 밤에 히든이 나오면 그것은 이미 결과물이라 재료가 하나 사라진다 — 이 값을 낮추면 첫 합성이 빨라지고 올리면 늦어진다. 히든 +0.30에 합성 −0.10이라 합계는 올랐다.

  0.20으로 더 올리지 않은 것은 그쯤이면 첫 밤이 다시 **히든이 몰리는 구간**이 되어 이 값을 만든 이유로 되돌아가기 때문이다.

  **맞는 값은 `HIDDEN_CHANCE × 평소 비율 ÷ 첫 밤 밭의 비율`이다.** 첫 밤 밭은 언제나 100%이므로 결국 평소 비율이 정하고, 그 비율은 아트가 올 때마다 움직인다 — **상수만 보고 고치면 또 어긋난다.**

- **한 판의 실측 (봇 60판, 목숨 3개 소진까지 · 2026-08-08).** 물건 107종·레시피 27개 상태.

  | | 값 |
  |---|---|
  | 판 길이 | 34.9초 |
  | 떨군 횟수 | 17.9 |
  | 쌓인 개수 | 13.6 |
  | 히든 | 판당 0.75개 · **하나도 못 본 판 50%** |
  | 합성 | 판당 0.27회 · **한 번도 못 한 판 77%** |
  | 특별한 것 합계 | 판당 1.02개 · 아무것도 못 본 판 40% |

  **떨군 횟수와 쌓인 개수를 헷갈리지 말 것.** 위의 "15개"는 쌓인 쪽이고, 히든 확률이 걸리는 것은 떨군 쪽이다. 목숨 3개를 잃는 동안 이탈한 것까지 세므로 떨군 쪽이 늘 많다.

  **합성이 히든의 부족분을 메워주지 못한다.** 왜 못 하는지를 두 단계로 갈라 쟀다(`PhysicsWorld`를 직접 돌려 120판 · 18드롭 · 중앙 조준).

  | 단계 | 판당 | 남는 비율 |
  |---|---|---|
  | 레시피 27개 | — | — |
  | 재료가 **살아 있는 채** 다 갖춰짐 | 0.66건 | 재료조차 안 갖춰진 판이 **48%** |
  | 그중 실제로 합쳐짐 | 0.20건 | 갖춰져도 **70%가 안 닿는다** |

  **2026-08-09에 다시 쟀다** (`tests/zz-contact.measure.test.ts`, 봇마다 40판 · 18드롭). 짝 표식이 들어와 "짝 옆을 노리는 봇"을 함께 돌릴 수 있게 됐다.

  | | 갖춤 | 합쳐짐 | **닿은 비율** | 이탈 |
  |---|---|---|---|---|
  | 중앙 조준 (표식 없던 때) | 0.47건 | 0.23건 | **47%** | 3.8개 |
  | 짝 옆 조준 (표식을 보는 사람) | 0.38건 | 0.23건 | **60%** | 3.6개 |

  **표식이 만든 실력 공간은 실재한다** — 닿는 비율이 47% → 60%다. 그런데 **판당 합성은 0.23으로 같다.** 짝 옆을 노리면 더 잘 닿지만 그만큼 빨리 소비되어 갖춤이 줄기 때문이다. 즉 지금 병목은 접촉이 아니라 **갖춤**이고, 한 번도 재료를 못 갖춘 판이 **63%**다.

  **재료 셋 이상은 80판 동안 갖춤 0.00 · 합성 0.00이다.** 앞서 곱셈으로 추정한 "재료 넷이 다 나올 확률 0.06%"와 맞는다(80판이면 기대값 0.05건). **"사실상 도달 불가능"이 아니라 측정된 0이다** — 밭을 좁히지 않는 한 결과물 17종은 도감에서 채울 수 없다.

  아래는 2026-08-08 값이다. 갖춤이 0.66 → 0.47로 내려간 것은 단어가 78 → 107개로 늘어서이고, 닿는 비율이 30% → 47%로 오른 것은 그 사이 받침대에 그릇 턱이 생겼기 때문으로 보인다.

  **병목이 둘이고 둘 다 비슷하게 무겁다.** 절반의 판은 재료가 아예 안 모이고(재료 30종이 단어 78개에 흩어져 있다), 모인 판에서도 열에 일곱은 닿지 않는다. 접촉을 100%로 만들어도 판당 0.66건에 그치고, 재료 갖춤을 두 배로 늘려도 접촉이 30%면 0.40건이다. **한쪽만 손대면 다른 쪽이 곧바로 막는다.**

  "갖춰졌다"는 **받침대에 살아 있는 것** 기준이다. 떨궜다고 있는 게 아니다 — 18번 떨구는 동안 3.9개가 받침대를 벗어나 사라진다.

  이 측정은 엔진이 아니라 `PhysicsWorld`를 직접 돌린 것이라 합성 횟수가 0.20으로, 엔진 실측 0.27보다 조금 낮게 나온다(목숨·판 종료를 흉내 내지 않았다). 비율을 보는 데는 쓸 수 있어도 절대값은 엔진 쪽을 믿을 것.
- 무거운 물건(`HEAVY_DENSITY = 1.8` 이상)은 텀블러 2.2 · 접힌 노트북 2.0 · 도시락 1.9다. 그 아래는 노트북 1.5로 간격이 뚜렷해서 "무거운데 안 흔들린다"가 생기지 않는다.
- **튐은 물건이 아니라 재질이 정한다** (`materials.ts`의 `BOUNCE`). 마찰·밀도와 갈라놓은 이유는, 튐이 물건 하나를 보며 고를 값이 아니기 때문이다 — 고무는 전부 튀고 천은 전부 죽는다. 예전에는 107종 중 **14종만** 값을 갖고 나머지 93종이 기본값 0.02로 완전히 같았다. 손끝에서 물건을 가르는 가장 강한 신호인데 비어 있던 축이다.

  | 재질 | 튐 |
  |---|---|
  | `rubber` | 0.34 |
  | `spark` | 0.16 |
  | `glass` | 0.14 |
  | `metal` | 0.12 |
  | `plastic` | 0.11 |
  | `wood` | 0.07 |
  | `tech` · `paper` | 0.03 |
  | `squish` | 0.02 |
  | `cloth` | 0.01 |

  값이 작은 것은 벽이 없는 받침대 때문이다. **튀면 곧 이탈이라 공짜가 아니다** — 중앙 조준 봇 24판으로 대가를 쟀다.

  | | 튐 없음 | 튐 넣고 |
  |---|---|---|
  | 버틴 드롭 | 17.2 | 16.8 |
  | 남은 물건 | 15.5 | 15.0 |
  | 최고 높이 | 2.20m | 2.15m |

  3% 안쪽이라 받아들였다. 물건별로 더 필요하면 `words.ts`에서 `restitution`을 적어 덮는다 — 축구공처럼 튐이 곧 정체인 물건이 그렇다. `tests/zz-bounce.measure.test.ts`가 값이 갈려 있는지와 봇이 버티는 길이를 함께 지킨다.

  **재질 표를 소리와 함께 쓰므로 분류를 옮기면 튐도 따라 바뀐다.** 분류 기준이 "귀에 들리는 결과"라 물질과 어긋나는 자리가 있는데(빛나는 것이 `glass`), 그것들은 애초에 실체가 없어 어느 값이든 무리가 없다.

  **높이를 잴 때 `stackTop()`을 쓸 것.** 물건 좌표를 그대로 훑으면 튀어올라 공중에 있는 것이 최고 높이로 잡혀서, 튐을 넣었을 때 쌓기가 좋아진 것처럼 보인다 — 실제로 1.75m가 2.74m로 나와 한 번 속았다.
- **밀도를 구간마다 나눠 봐야 하는 값이 있다 — 히든이 그렇다.** 첫 밤에는 밭이 좁혀져 있고 그 밭은 **히든 보유 단어만으로** 이루어진다(같은 것 둘의 결과물이 곧 그 단어의 히든이니 구조적으로 그렇다). 아래 표를 잴 때는 평소가 78단어 중 11개(14%)여서 같은 확률이라도 밀도가 일곱 배로 뛰었다. **지금은 아트가 늘어 107단어 중 37개(35%)이므로 그 배율이 셋에 못 미친다** — 값을 다시 만질 때는 배율부터 다시 재야 한다.

  | 첫 밤 확률 | 첫 합성까지 | 첫 밤 히든 | 판당 히든 |
  |---|---|---|---|
  | 0.34 (`HIDDEN_CHANCE` 그대로) | 8.8번 | **2.42** | **2.87** |
  | 0.10 | 7.3번 | 0.80 | 1.31 |
  | **0.05 (지금)** | **6.6번** | 0.42 | **0.97** |
  | 0 | 6.2번 | 0 | 0.57 |

  첫 밤의 밭을 넣기 전 실측이 판당 0.75개였으니 `OPENING_HIDDEN_CHANCE = 0.05`가 그 자리로 돌려놓는 값이다. **전체 확률을 낮춰 풀면 안 된다** — 원인이 확률이 아니라 밭의 구성이라, 전체를 건드리면 첫 밤 뒤의 구간까지 함께 마르고 그쪽은 이 프로젝트가 "히든이 너무 적다"에서 시작했던 자리다. 0으로 두지 않은 것은 첫 밤에도 "무엇이 나올까"가 남아야 해서인데, 히든이 나오면 그것은 이미 결과물이라 재료가 하나 사라진다 — 그래서 이 값을 낮추면 첫 합성도 함께 빨라진다.
- 중력은 -7로 지구보다 약하다. 실제 중력이면 착지 속도가 8m/s를 넘어 쌓아둔 것을 밀어낸다.
- **빈 받침대 중앙에 떨궜을 때 저절로 떨어지는 물건이 있으면 안 된다.** `tests/PhysicsWorld.test.ts`가 모든 변형에 대해 지킨다. 이 테스트가 우산 굴러떨어짐을 잡았다.

## 스프라이트 파이프라인

`assets-src/**/*.png`(원본) → `scripts/prepare-sprites.cjs` → `public/items/*.webp` + `src/game/data/sprites.generated.ts`

**[docs/SPRITES.md](docs/SPRITES.md)에 전부 있다** — 단계, 새 아트를 넣는 절차, 그리고 밟은 함정(한 변만 적는 것의 대가, 여러 덩어리, 단순화 강도, 볼록 검사 시점, 메워지는 구멍). 한쪽에만 두는 이유는 형식이나 상한을 바꿀 때 다른 쪽을 잊기 때문이다 — `.png`에서 `.webp`로 갈아탈 때 실제로 겪었다.

생성 파일은 직접 고치지 말고 스크립트를 다시 돌린다. 스크립트는 조각 면적 합이 실루엣과 맞는지, 모든 조각이 볼록한지 검증하고 어긋나면 중단한다.

### 새 물건의 크기는 CSV의 등급에서 온다

원본 목록의 `canvas_occupancy`가 다섯 등급이다(20% · 37.5% · 72.5% · 80.5% · 92%). 오래 이것이 게임에 반영되지 않았다고 여겼는데, 124종을 등급별로 갈라 재보니 **이미 맞아 있었다.**

| 등급 | 종수 | 지금 게임의 큰 변 (평균) |
|---|---|---|
| 20% | 21 | 0.40 ~ 0.69 (**0.47**) |
| 37.5% | 45 | 0.55 ~ 0.88 (**0.64**) |
| 72.5% | 16 | 0.70 ~ 0.94 (**0.78**) |
| 80.5% | 15 | 0.82 ~ 1.06 (**0.89**) |
| 92% | 7 | 0.80 ~ 1.13 (**0.99**) |

범위는 겹치지만 평균이 단조로 오른다. 손으로 하나씩 정한 값들이 결국 등급을 따라간 것이라, **새 물건은 그 평균을 출발점으로 쓰고 물건의 사정에 따라 조금씩 민다** — 키보드는 넓으니 0.64 대신 0.70, 손 연필깎이는 아주 작으니 0.47 대신 0.42다.

값을 적는 축은 **긴 변**이다. 짧은 변을 적으면 비율에 곱해져 긴 변이 등급 밖으로 튄다.

### 물건 이름에는 길이 제한이 없다 — 굽히는 쪽은 이름표다

이름표(`nicknames.ts`)가 물건 이름을 그대로 뒷자리로 쓰는데 앞에 꾸미말 넉 자와 띄어쓰기가 붙고 전체 상한이 12자다. 히든을 붙이며 "곰돌이 오므라이스"·"아이스 아메리카노"·"디지털 알람시계" 셋이 여기에 걸렸다.

**물건 이름을 줄여서 풀면 안 된다.** 이 게임은 타자게임이고 긴 이름이 곧 재미다 — 물건마다 이름 길이가 다른 것이 의도다. 그래서 `nouns()`가 **긴 이름을 재료에서 걸러낸다.** 그 물건은 이름표로 못 쓰이지만 판에서는 그대로 나온다.

거르는 기준(`NOUN_MAX`)은 적어두지 않고 **가장 긴 꾸미말과 상한에서 계산한다.** 꾸미말을 늘리거나 상한이 바뀌면 저절로 따라 움직인다. `tests/nicknames.test.ts`가 "거를 것이 실제로 있는지"까지 지킨다 — 걸러낼 대상이 없으면 그 검사는 아무것도 지키지 않는다.

## 합성

재료가 서로 닿으면 합쳐진다. 레시피는 `game/data/recipes.ts`의 배열이고 재료 수·종류를 가리지 않는다.

**결과물이 사는 곳은 둘이다.**

같은 물건 둘을 합친 결과는 **그 물건의 히든**이다(피자 조각 둘 → 피자 한 판). 운으로도 만나고 손으로도 만드는, 얻는 길이 둘인 물건이다.

서로 다른 물건을 합친 결과는 재료 어느 쪽의 다른 형태도 아니다 — 계란 프라이는 계란의 다른 형태가 아니고 금메달은 번개의 다른 형태가 아니다. 어느 단어에 매달아도 거짓말이 되므로 `words.ts`의 **`CRAFTED`**에 따로 산다. 타이핑으로는 절대 떨어지지 않는다(`resolveItem`은 `WORDS`만 본다).

**"물건이면 다"를 뜻하는 자리는 `ALL_VARIANTS`를 쓴다.** 도감·스프라이트 미리받기·재질 표가 그렇다. `WORDS`만 훑으면 합성 전용 40종이 조용히 빠지는데, 그 실패는 도감에 칸이 안 생기거나 합성한 순간 그림이 없는 것으로 한참 뒤에야 드러난다.

**합성의 보상은 판 안의 자리가 아니라 판 밖에 남는 것이다.** 도감의 빈 칸이 채워지고, 그 히든을 봤다는 사실이 남고, **프로필 사진으로 쓸 수 있다**(아이콘은 도감에서 모은 물건 중에서만 고른다).

### 합성해도 무엇이 나올지는 모른다

레시피 다섯은 결과를 **여럿** 갖는다(`Recipe.hiddenResults`). 우주선의 비행접시, 여행가방의 빈티지 트렁크, 마법봉의 날개 마법봉, 테라리움의 행잉형, 하트반지의 다이아반지.

단어에 매달린 물건이 기본형과 히든을 갖는 것과 같은 구조인데, 합성 결과물은 단어가 없어 그 목록을 가질 수 없었다. **아트는 처음부터 그것들의 다른 형태를 그려 보내고 있었다** — 다섯 장이 붙일 자리가 없어 떠 있다가 이 구조로 들어왔다.

뽑는 것은 `resolveCrafted`이고 **확률은 히든과 같은 값**(`HIDDEN_CHANCE`)이다. 얻는 경로가 다를 뿐 플레이어에게는 "무엇이 나올까"라는 같은 종류의 사건이다. 판의 난수를 쓰므로 같은 시드면 같은 결과다.

`resolveItem`과 합치지 않은 것은 **찾는 방법이 다르기 때문이다.** 그쪽은 단어로 표를 뒤지고 이쪽은 이미 정해진 레시피를 받는다. 뽑는 규칙만 같다.

**도감이 `CRAFTABLE_IDS`로 만들 수 있는 것을 센다.** 다른 형태를 거기 넣지 않으면 칸은 있는데 채울 길이 없는 물건이 된다. `recipeFor`도 다른 형태를 같은 레시피로 이어야 도감에서 재료가 비어 보이지 않는다.

### 재료 셋 이상은 아직 도달 불가능하다

레시피 44개 중 **17개가 재료 셋에서 여섯**이다(2026-08-09에 아트와 함께 들어왔다). 그리고 지금 그것들은 사실상 만들 수 없다.

단어가 107개이고 한 판에 18번쯤 떨어지므로 **특정 단어 하나가 한 판에 나올 확률이 15.6%**다. 재료가 넷이면 넷이 다 나올 확률이 0.06%, 여섯이면 0.0014%다. 그 위에 "재료가 서로 닿아 있어야 한다"가 또 걸리는데 **둘이 닿는 것도 30%**다.

**확률로 메울 수 있는 간격이 아니다.** `HIDDEN_CHANCE`를 두 배로 올려도 0.06%가 0.12%가 될 뿐이다. 자릿수를 옮기는 것은 **밭의 구성**뿐이다 — 후보를 6개로 좁히면 특정 단어가 나올 확률이 96%가 되고 넷을 모을 확률이 86%가 된다. 판 앞머리에서 이미 한 번 확인한 구조이고, `WordSpawner.restrict()`가 그 장치다.

**그럼에도 레시피를 먼저 넣은 이유는, 없는 것은 재지 못하기 때문이다.** 어떤 장치가 얼마나 필요한지는 도달률을 재봐야 알 수 있다.

**여덟은 재료에 다른 합성 결과물이 들어간다**(여행가방·레이싱 깃발·우주선·계란 프라이·구멍 난 셔츠·햇빛·금메달·마법봉). 사슬이 두 단이라 더 어렵다.

푸는 순서는 백로그에 있다 — ① 재료를 화면에 알린다 ② 짝 옆을 노리는 봇으로 접촉률을 다시 잰다 ③ 그다음 밭을 좁힌다. **②를 건너뛰면 안 된다.** 지금의 30%는 중앙만 보고 떨구는 봇으로 잰 값이라 실력의 한계가 아니라 정보가 없어서 나온 숫자다.

예전에는 **결과물이 재료보다 좁아야 한다**는 규칙이 있었다 — 벽이 없으니 폭이 곧 위험이고 합성이 자리를 틔워주면 그것이 보상이라는 생각이었다. 2026-08-09 재작화에서 버렸다. 여행앨범이 세로형에서 가로형으로 다시 그려져 폭이 0.461 → 0.855가 되자 규칙을 지키려면 **아트가 정한 크기를 줄여야** 했는데, 물건마다 크기가 다른 것이 이 게임의 컨셉이라 크기가 이겼다. 보상이 수집이라면 결과물이 넓어져도 합치고 싶은 마음은 사라지지 않는다.

남은 제약은 하나다 — **결과물도 조준 범위(`MAX_ITEM_HALF_WIDTH`)를 넘지 않아야 한다.** 그쪽은 취향이 아니라 즉사를 막는 것이고 `tests/Merger.test.ts`가 지킨다.

### 판의 시간 — 첫 밤 · 낮 · 밤

판은 **밤에서 시작해** 낮과 밤을 번갈아 돈다(`systems/DayNight.ts`). 시간이 정하는 것은 **어떤 단어가 내려오는가** 하나뿐이다 — 낙하 속도도 밀도도 시간과 무관하다(그쪽은 쌓은 높이가 정한다). 압박의 축을 둘로 늘리면 서로 겹쳐서 어느 쪽이 판을 끝냈는지 알 수 없게 된다.

| 국면 | 길이 | 내려오는 단어 | 왜 |
|---|---|---|---|
| 첫 밤 | 15초 | 정해둔 재료 **둘** (`Opening.ts`) | 치는 족족 짝이 갖춰진다. 합성을 배우는 구간 |
| 낮 | 30초 | 전부 (107종) | 판의 보통 상태 |
| 밤 | 15초 | 재료만 (79종, `NightWords.ts`) | 몰아서 합칠 수 있는 구간 |

**첫 밤은 한 번뿐이다.** 배우는 구간이 되풀이되면 그때부터는 배울 것이 없는데 밭만 좁아진 구간이 된다. 낮부터 시작하지 않는 이유는 재료가 107개 단어에 흩어져 있어 첫 합성까지 한참 걸리고, 그 사이에 이 게임이 무엇을 하는 게임인지 배우지 못한 채 판이 끝나기 때문이다.

**국면이 바뀌어도 이미 내려오는 단어는 그대로 둔다.** 사라지면 치려던 것이 손 아래에서 없어진다. 밭은 다음에 뽑을 것만 정한다.

**"밭이 좁다"를 첫 밤의 표식으로 쓰면 안 된다.** 밤에도 밭을 좁히므로 `WordSpawner.restricted`는 **첫 밤과 밤 둘 다에서 참**이다. 히든 확률이 실제로 이 플래그를 보고 있었고, 그래서 몰아치라고 만든 밤이 오히려 히든이 가장 마르는 구간이 됐다(0.34 → 0.05). 눌러야 할 근거는 좁다는 것이 아니라 **밭이 전부 히든 보유 단어라는 것**인데, 첫 밤의 밭은 구조적으로 100%지만 밤의 밭은 79종 중 26종(33%)으로 낮(35%)과 다를 게 없다. 지금은 `phaseNow === 'firstNight'`로 본다. 국면에 딸린 값을 새로 만들 때마다 같은 자리다 — `restricted`가 아니라 국면을 보라.

**히든이 눈에 보이는 비율은 확률 그대로가 아니다.** 히든이 없는 단어를 쳤으면 롤에 걸려도 나올 것이 없어서, 관측값은 `확률 × 히든 보유 단어 비율`이다. 밤의 밭은 그 비율이 33%라 0.34로 굴려도 11%쯤으로 보인다 — 확률을 기대값으로 그대로 쓴 회귀 테스트가 고쳐놓은 코드에서 실패했다. `tests/NightHidden.test.ts`가 이 둘을 함께 지킨다.

**해가 지고 뜨는 것은 순간이 아니다.** `nightfall`이 국면 경계에서 `TWILIGHT_SEC`(2.5초)에 걸쳐 이어서 넘어간다. 밤보다 넉넉히 짧아야 하는데, 저물자마자 다시 밝아지면 밤 한복판조차 완전히 어둡지 않아서 밤에 온 것이 아니라 스쳐 지나간 것이 된다.

#### 첫 밤에는 밭을 둘로 좁힌다

합성은 만들어놓고 **거의 아무도 못 보는 기능**이었다. 중앙 조준 봇으로 25번을 떨궈도 40판 중 **7판**만 첫 합성에 닿았다 — 재료 30종이 단어 78개에 흩어져 있어 짝이 받침대에 동시에 살아 있을 일이 드물다.

그래서 첫 밤에는 **쉽게 합쳐지는 단어 2개만** 내보낸다(`systems/Opening.ts`). 같은 물건 둘짜리 재료라 한 단어를 두 번 치면 갖춰지고, 도형이 같아 잘 붙는다.

| 밭 | 25번 안에 첫 합성 | 중앙값 | 이탈 |
|---|---|---|---|
| 78종 (좁히기 전) | **7 / 40판** | — | 10.3개 |
| 7종 | 36 / 40 | 10번 | 4.6개 |
| 3종 | 39 / 40 | 9번 | 2.5개 |
| **2종 (지금)** | **40 / 40** | **7번** | 1.9개 |
| 1종 | 40 / 40 | 5번 | 1.6개 |

**한때 셋이었다.** 초반 동시 낙하 상한이 셋이라(`Difficulty.ts`의 `OPENING`) 화면에 뜨는 단어 수가 좁히기 전과 같고, 달라지는 것은 "같은 단어가 되풀이된다"뿐이어서 손이 멈추지 않는다는 이유였다. **둘로 더 좁힌 것은 첫 밤이 15초로 짧기 때문이다** — 너덧 단어뿐이라 셋이면 그 안에 같은 단어가 두 번 나오지 않는 판이 생기고, 배우라고 만든 구간에서 아무것도 못 배운 채 낮으로 넘어간다. 대가는 가끔 칠 것이 없어 기다리는 것이고, 하나까지 줄이지 않는 이유가 그것이다.

**푸는 조건은 시간이다.** 예전에는 첫 합성이 풀어줬는데, 국면이 도는 지금은 합성 하나로 시계를 앞당기면 밭과 화면이 어긋난다(밤 배경인데 낮 단어가 내려온다).

**끈적한 물건은 뺀다.** `sticky`는 닿은 것을 고정 관절로 묶는데, 첫 밤은 쌓기를 배우는 구간이고 밭이 좁아 같은 물건이 되풀이되므로 하나만 섞여도 판 전체가 굳어 "이 게임은 잘 안 무너진다"를 먼저 배운다. **기본형과 히든을 함께 본다** — 같은 것 둘의 결과물이 곧 그 단어의 히든이라 히든만 끈적해도 첫 합성의 보상이 붙어버리는 물건이 된다(소시지 → 문어소시지). 달팽이는 기본형부터 끈적하다. 둘을 빼도 후보가 다섯 남고, 오히려 첫 합성이 39/40·9번에서 **40/40·7번**으로 빨라졌다.

**대전에는 걸지 않았다.** 밭을 방장이 소유하므로 걸면 두 사람이 같은 두 단어를 치게 된다.

## 소리

**오디오 파일은 하나도 없다.** 전부 WebAudio로 합성한다(`src/audio/`). 번들이 늘지 않고 라이선스도 얽히지 않지만, 그것보다 **값을 이어붙일 수 있는 것**이 이유다 — 부딪힌 세기가 곧 음량과 길이가 되고 물건의 크기가 곧 음높이가 된다. 녹음으로 같은 것을 하려면 세기 단계마다 파일을 두고 골라야 한다.

| 파일 | 하는 일 |
|---|---|
| `game/types/events.ts` | 사건 타입. 브라우저를 모른다 |
| `game/data/materials.ts` | 물건 185종의 재질 표와 개체값 |
| `audio/AudioBus.ts` | 컨텍스트·마스터·컴프레서. WebAudio를 아는 유일한 자리 |
| `audio/outputLevels.ts` | 최종 출력 배율과 그 배율에서 제외할 소리의 보정값 |
| `audio/voices.ts` | 소리 하나하나의 합성 |
| `audio/Bgm.ts` | 배경음악 재생기 |
| `audio/tracks.ts` | 배경음악의 악보. 화면마다 한 곡 |
| `audio/SoundLimiter.ts` | 몇 개를 흘려보낼지. 순수 로직이라 node에서 테스트가 돈다 |
| `audio/SoundBoard.ts` | 사건 → 소리. 페이지에 하나뿐인 싱글턴 |

### 물건마다 다른 소리

**재질 표가 소리를 정한다** (`game/data/materials.ts`). 물건 전체를 부딪혔을 때 나는 소리로 열 갈래로 나눴다 — 실제 물질이 아니라 **귀에 들리는 결과**가 기준이라, 나뭇잎과 피자 박스가 같은 `paper`이고 스테인리스 텀블러와 자전거가 같은 `metal`이다.

| 재질 | 소리 |
|---|---|
| `glass` | 정수배 배음이 맑게 길게 남는다 |
| `metal` | 정수배가 아닌 배음(2.76 · 5.4)이 어긋난 채 울린다 |
| `wood` | 배음 없이 짧게 끊기는 "톡" |
| `paper` | 몸통이 거의 없고 잡음이 전부인 "바스락" |
| `cloth` | 가장 조용한 "푹". 얹혔다는 것만 겨우 알린다 |
| `plastic` | 울림 없는 마른 "딱" |
| `rubber` | 음높이가 훅 떨어지는 "통" |
| `tech` | 무겁게 내려앉고 끝에 금속이 한 번 |
| `squish` | 음높이가 아래로 미끄러지는 "퍽" |
| `spark` | 번개. 물건이 아니다 |

착지음은 **여섯 가지**가 함께 정한다 — 박스가 실제 질량에 따라 내는 반응, 재질(배음과 울림), 물건 크기(몸통 음높이, 큰 것이 낮게), 부딪힌 세기(음량과 길이), 개체값 `tone`(같은 재질 안에서 몇 반음 밀기), 개체값 `grain`(울림 길이와 잡음 밝기). 낙하음은 재질마다 훑는 대역이 다르다: 무엇이 떨어지는지가 **닿기 전에** 들려야 한다 — 물건의 정체는 Enter를 친 순간 처음 공개되는데 그때 눈은 다음 단어를 쫓고 있다.

**박스 착지음은 실제 질량으로 넷으로 갈린다.** `PhysicsWorld`가 콜라이더까지 반영한 질량을 사건에 싣고, `voices.ts`가 `<0.08` 사뿐 · `<0.18` 풀썩 · `<HEAVY_MASS(0.35)` 척/탁 · 그 이상 쿵으로 바꾼다. 185종을 재면 각각 41 · 64 · 42 · 38종이다. 중간 무게의 척/탁은 난수가 아니라 `grain`으로 갈라 같은 물건이 늘 같은 감각을 갖는다. 이 층은 박스의 반응이고, 기존 재질음은 무엇이 떨어졌는지를 말하므로 둘을 겹친다.

**사무실 나무문 소리 둘은 스플래시에서 혼자 하기로 넘어가는 전환용이다.** 버튼을 누르면 `woodenDoorOpen`이 경첩을 끼익 열고 화면이 0.26초 동안 검어진다. 열림의 가장 긴 마찰이 0.55초라 검은 화면을 0.34초 유지해 합계 0.6초를 맞춘 뒤, 완전히 가린 틈에서 플레이 화면으로 바꾸며 `woodenDoorClose`가 문짝과 잠금쇠를 쿵 닫는다. 새 화면은 0.22초에 걸쳐 드러난다. 첫 제스처와 전환이 겹쳐도 열림을 잃지 않도록 컨텍스트가 열릴 때까지 하나만 보관한다(`SoundBoard.setSplash`) — 게임 사건까지 보관하면 돌아왔을 때 지난 충돌음이 터지므로 문만 예외다. 게임 아이템 `wooden-door`는 다른 나무 물건처럼 일반 낙하음과 착지음을 쓴다.

**재질 표를 `words.ts`가 아니라 따로 둔 이유**는, 소리를 조율할 때 보아야 하는 것이 물건 하나가 아니라 무리이기 때문이다. 유리끼리 비슷하고 나무끼리 비슷하되 서로는 확실히 달라야 하는데, 그 균형은 표를 통째로 볼 때만 잡힌다. 마찰·밀도는 물건 하나를 보며 정하므로 `words.ts`에 남아 있다. **튐(`restitution`)만은 이 표가 정한다** — 아래 밸런스 항목에 이유가 있다.

**개체값은 해시가 아니라 등간격이다.** 처음엔 id 해시를 그대로 썼는데 뭉치는 자리가 생겼다 — 물컹한 것 13종 중 둘이 0.0016 차이(0.01반음)로 붙어 완전히 같은 소리였다. 물건이 늘수록 이런 짝은 반드시 나온다(생일 문제). 그래서 **순서만 해시로 섞고 자리는 등간격으로 나눈다.**

#### 개체값은 줄이 아니라 격자다

등간격으로 폈어도 **한 줄에 21개를 밀어 넣으면 결국 좁다.** 귀에 닿는 간격은 `재질의 폭 ÷ 종수`(반음)인데, 그렇게 재면 이랬다.

| 재질 | 종수 | 폭 | 예전 간격 | 격자 | 지금 |
|---|---|---|---|---|---|
| metal | 21 | 10반음 | 0.48 | 5×5 | **2.00** |
| squish | 18 | 9 | 0.50 | 5×4 | 1.80 |
| cloth | 13 | 3 | **0.23** | 4×4 | 0.75 |
| paper | 12 | 4 | 0.33 | 4×3 | 1.00 |
| glass | 11 | 9 | 0.82 | 4×3 | 2.25 |

가장 나쁜 것은 물건이 많은 금속이 아니라 **폭이 좁은 천**이었다. 등간격이라는 것만 지켰지 0.23반음은 같은 소리다.

폭을 넓히는 것으로는 못 푼다. 넓히면 같은 재질이 한 무리로 들리지 않게 되는데, 유리끼리 비슷한 것이야말로 재질을 나눈 이유다. 그래서 **축을 하나 더 만든다** — 음높이(`tone`)와 울림의 결(`grain`: 길이와 잡음 밝기)로 격자를 짜고 물건을 칸마다 앉힌다. 폭은 그대로 두고 갈라지는 수만 늘린 것이고, 축이 둘이면 필요한 칸 수가 제곱근으로 줄어든다.

**두 축을 반대로 묶는다.** `grain`이 높으면 길게 울리고 잡음이 어둡다(크고 속 빈 것), 낮으면 짧고 밝다(작고 단단한 것). 같은 방향으로 밀면 "밝고 긴 것"과 "어둡고 짧은 것"뿐이라 결국 축이 하나로 되돌아간다.

**몸통이 없는 재질은 잡음 쪽에 몫을 더 준다.** 마른 것(`bodyGain` 0.18)과 천은 울림 길이를 밀어도 들리지 않아서, `tickSpread`가 `ringSpread`보다 크다. 다만 이미 밝은 잡음(마른 것 2.6kHz, 번개 4.2kHz)은 더 열면 뾰족해지므로 그쪽만 폭을 줄였다.

낙하음도 같은 `tone`을 쓰므로 함께 벌어졌다(금속 0.19 → 0.8반음). 격자 하나로 둘이 같이 풀린다.

`tests/materials.test.ts`가 지킨다 — `(tone, grain)` 쌍이 겹치지 않을 것, 같은 음높이 칸끼리 **실제 반음 간격이 0.6 이상**일 것(폭까지 곱해서 본다), 음높이가 같으면 울림이 다를 것, 모든 물건이 표에 있을 것.

### 음량은 둘로 나뉜다

효과음과 배경음악. 하나였을 때는 "음악이 거슬린다"와 "소리가 크다"를 구분할 수 없었는데, 해법이 정반대인데도 방법이 하나뿐이라 음악을 줄이려면 필요한 효과음까지 같이 줄여야 했다. 이 게임에서 효과음은 장식이 아니라 **얹혔는지 알려주는 정보**다.

**전체 음량은 두지 않는다.** 둘의 곱일 뿐이라 같은 일을 두 군데서 하게 되고, 전체를 줄였는데 효과음이 이미 0이면 아무 일도 일어나지 않아 설정이 고장난 것처럼 보인다.

배경음악과 효과음은 사용자 값에 `PERCEIVED_LOUDNESS_BOOST`(3.2)를 함께 반영한다. 처음에는 요청의 "2배"를 진폭 2배(+6dB)로 옮겼지만 실기에서는 여전히 작았다. **진폭과 체감 음량은 선형이 아니다** — 귀에 두 배쯤 크게 들리려면 약 +10dB, 진폭 3.16배가 필요해서 3.2로 반올림했다. 배경음은 기존 비율 0.38에 이것을 곱한 1.216이고 효과음은 3.2다. 둘의 상대적인 균형은 그대로이며, 겹쳐 피크가 커지는 순간은 컴프레서가 잡는다.

그 뒤 실제로 들리는 전체 출력을 `FINAL_OUTPUT_GAIN`(1.3)으로 한 번 더 올린다. **컴프레서 뒤에 두는 것이 핵심이다** — 앞에서 올리면 큰 소리가 다시 눌려 요청한 1.3배가 스피커까지 가지 않는다. 히든 발견의 "띵"은 이미 충분히 커서 `HIDDEN_REVEAL_PRE_GAIN`(1 ÷ 1.3)을 먼저 곱해 최종 출력에서 이전 음량으로 돌아오게 한다. 예외를 버스 분기로 만들지 않은 것은 이 소리 하나뿐이라 통로를 하나 더 유지하는 비용이 더 크기 때문이다.

옵션 화면은 `useSoundMenu()`가 주는 목록을 그대로 그리므로, **항목을 늘려도 화면은 손대지 않는다.**

### 배경음악

곡은 `tracks.ts`의 표이고 재생기는 그것을 읽기만 한다 — 곡을 하나 더 넣는 것이 표를 하나 더 쓰는 일이어야 하고, 그렇지 않으면 재생기가 화면을 알게 된다.

| 화면 | 곡 | 중심음 | 빠르기·길이 | 표식 | 멜로디 |
|---|---|---|---|---|---|
| 낮 스플래시 · 옵션 | `splashDay` | D장조 | 72 BPM · 12마디 | 사인 플럭, 가벼운 중역 타격 | 있다 |
| 밤 스플래시 · 옵션 | `splashNight` | B단조 | 108 BPM · 12마디 | 성긴 삼각파, 낮은 타격 | 있다 |
| 낮 플레이 · 대전 | `gameDay` | G장조 | 96 BPM · 16마디 = 40초 | 밝은 짧은 타악, 베이스 2연타 | **없다** |
| 밤 플레이 | `gameNight` | E단조 | 116 BPM · 20마디 ≈ 41초 | 낮은 타악, 베이스 3연타 | **없다** |
| 대전 대기방 | `lobby` | D도리안 | 76 BPM · 8마디 | **사각파**, 패드 없음 | 있다 |
| 도감 | `collection` | F장조 | 66 BPM · 8마디 | **3/4 왈츠**, 오르골 | 있다 |

낮·밤 네 곡은 참고 음원의 멜로디를 옮기지 않고 **빠르기·밝기·리듬 밀도만** 분석해 새로 썼다. 지정 구간의 주된 박은 낮 스플래시 약 62, 밤 스플래시 약 100, 낮 플레이 약 80, 밤 플레이 약 105 BPM으로 측정됐고, 각각 72 · 108 · 96 · 116 BPM으로 한 걸음씩 올렸다. 화음 진행과 선율은 새것이며, 플레이 둘은 기존 원칙대로 선율을 비워 효과음을 앞에 남겼다.

참고 구간은 짧은 타격이 곡의 결을 크게 갈랐는데 기존 재생기는 음정층만 네 겹이었다. 그래서 `BgmTrack.rhythm`을 더했다 — 0~1 세기표대로 흰 잡음을 아주 짧게 잘라 플럭 아래에 놓는다. 음량은 0.007~0.011이라 착지음보다 한참 뒤다.

**어느 화면에서 무엇이 흐르는지는 `App.tsx`의 `musicFor` 한 곳에서 정한다.** 스플래시는 그림과 같은 `TitleTheme`, 싱글은 단어 밭과 배경이 이미 쓰는 `DayNight.phase`를 받는다. 첫 밤도 밤 곡이다. 대전은 낮에 머무르므로 `gameDay`만 쓴다. 화면마다 각자 시계를 읽거나 곡을 부르면 경계에서 그림과 음악이 다른 낮·밤을 말한다.

**2026-08-10 브라우저에서 실제 재생기까지 확인했다.** 혼자 하기 첫 밤의 `SoundBoard.wantedTrack`과 `Bgm.playing`이 모두 `gameNight`였고, 실행 중 악보도 116 BPM · 20마디 · 사인 아르페지오 · 삼각파 베이스였다. 예전 84 BPM `game` 키는 현재 `TRACKS`에 없다. 새 곡이 예전처럼 들린다면 연결 회귀가 아니라 같은 WebAudio 합성기와 선율 없는 플레이 곡이라는 음색 설계의 문제다.

### 곡이 바뀔 때

**곡마다 자기 게인 노드를 갖는다.** 자연스럽게 넘어가려면 두 곡이 잠시 동시에 울려야 하는데, 게인 하나를 나눠 쓰면 그 구간에서 둘의 음량을 따로 움직일 수 없다 — 옛 곡을 줄이면 새 곡도 같이 줄어든다.

| 상황 | 시간 | 왜 |
|---|---|---|
| 앱의 첫 곡 | **0.24초** | 첫 제스처가 혼자 하기면 스플래시는 0.6초 뒤 가려진다. 1.4초로 열면 음량이 절반에도 못 닿아 없는 곡처럼 들린다 |
| 곡 → 다른 곡 | **1.4초** 크로스페이드 | 화음 한 마디쯤. 옛 곡이 한 번 더 마무리하고 물러나는 것으로 들린다 |
| 곡 → 조용함(일시정지) | **0.45초** | 멈췄다는 것이 바로 전해져야 한다. 1초 넘게 끌면 멈춘 뒤에도 판이 이어지는 것처럼 들린다 |
| 조용함 → 곡 | 1.4초 | 일시정지에서 돌아올 때 시작이 튀지 않게 한다 |

**물러나는 곡도 겹치는 동안 계속 연주한다.** 예약을 바로 끊으면 그 구간에서 옛 곡이 화음만 남아 앙상해져서, 부드럽게 넘어가는 것이 아니라 한쪽이 고장난 것처럼 들린다.

**같은 곡을 다시 틀면 있던 자리에서 이어간다.** 일시정지했다 돌아올 때마다 첫 마디부터 시작하면 그 자체가 "끊겼다"는 신호가 되고, 자주 멈추는 사람은 곡의 앞부분만 듣게 된다. 자리는 물러나기 시작할 때가 아니라 **예약을 끊는 순간**에 적어야 한다 — 겹치는 1.4초 동안에도 계속 연주하기 때문이다.

램프를 걸 때는 `cancelAndHoldAtTime`으로 **지금 값을 붙잡고** 시작한다. 그냥 걸면 앞서 걸어둔 램프가 살아 있어서, 밀려들어오는 중에 곡이 또 바뀌면 음량이 튄다 — 화면을 빠르게 오갈 때 실제로 밟는 경로다.

### 곡을 무엇으로 가르는가

처음에는 넷을 **빠르기와 화음표만 바꿔** 만들었다. 실기 평은 "너무 다 같은 배경음 같다"였고 당연한 결과였다 — 음색이 같고(사인 베이스 + 삼각파 패드 + 삼각파 아르페지오), 박자가 같고(8분음표 여덟 칸), 무엇보다 **멜로디가 없었다.** 화음만 깔린 것들끼리는 코드 진행을 바꿔도 "같은 배경음"으로 뭉뚱그려 들린다.

가르는 축은 넷이고, **하나만 바꾸는 것으로는 부족하다.**

1. **멜로디** — 가장 큰 표식. 없으면 전부 "화음 깔린 배경"이 된다
2. **음색** — 파형, 패드의 유무, 음 길이(스타카토/레가토), 옥타브
3. **박자** — 3/4는 4/4와 걸음걸이 자체가 달라서 조성보다 먼저 "다른 곡"으로 들린다
4. **중심음** — 마지막에 온다. 이것만 바꾸면 거의 티가 안 난다

**판에만 멜로디를 두지 않는다.** 판에서 귀가 쓸 정보는 얹혔는지·놓쳤는지이고 멜로디는 그 주의를 가져간다. 없는 것 자체가 판의 성격이 된다.

**조성은 갈라도 음 집합은 붙여둔다.** C장조·A단조·D도리안은 같은 흰건반 일곱 음이고 F장조만 Bb 하나가 다르다. 곡을 바꿀 때 예약된 음을 끊지 않으므로 앞 곡의 여운 위로 다음 곡이 시작하는데, 음 집합이 붙어 있으면 그 겹침이 부딪히지 않는다.

`tests/tracks.test.ts`가 이 구분을 지킨다 — 여섯 곡의 빠르기가 다를 것, 박자가 하나로 통일되지 않을 것, 아르페지오 음색이 셋 이상으로 갈릴 것, 스플래시는 선율이 있고 플레이 둘만 없을 것, 낮·밤의 중심음과 리듬이 다를 것.

플레이 곡이 40초를 넘는 이유는 **한 판이 1분을 넘기기 때문**이다. 처음 4마디(11초)로 뒀더니 같은 자리가 다섯 번 넘게 돌아왔고, 그쯤 되면 음악이 배경이 아니라 시계처럼 들린다. 늘릴 때는 길이만 늘리지 말고 **단락 끝의 화음을 다르게** 둔다 — 어디쯤인지 알 수 있어야 길이가 길이로 들린다. `tests/tracks.test.ts`가 길이를 지킨다.

### 날카로움은 음량이 아니라 고역과 어택이다

첫 판을 실기로 들었을 때 "전체적으로 너무 뾰족하다"가 나왔다. 원인은 둘이었고 **둘 다 음량이 아니었다.**

1. **고역.** 오실레이터의 배음과 잡음의 윗부분이 그대로 나오면 3~6kHz에 에너지가 몰리는데, 사람 귀가 가장 예민한 대역이 정확히 거기다. 소리마다 값을 낮추면 작아지기만 하고 여전히 뾰족하다. 그래서 **효과음 버스에서 한 번에 깎는다**(`highshelf −8dB @ 2.4kHz` → `lowpass 5.2kHz`). 버스에 두면 새 소리를 추가할 때도 저절로 같은 성질을 갖는다.
2. **어택.** 0에서 최대로 순간에 뛰면 그 자체가 클릭 잡음이 된다. `tone`/`burst`의 기본 어택을 올렸다(0.005 → 0.012).

개별 소리에서도 같은 원칙으로 손봤다 — 톱니파·사각파를 삼각파/사인으로, 타자음을 2.6kHz 밴드패스에서 760Hz 로우패스로(자주 나는 소리일수록 낮고 둔해야 한다), 히든 종소리에서 5.4배 배음(5.6kHz)을 제거, 메뉴음을 한 옥타브 반 아래로.

**`lowpass`의 Q를 높이지 말 것.** 차단 지점이 오히려 솟아올라 그 대역이 새로 뾰족해진다. 지금 0.4다.

### 밟은 함정

- **컨텍스트는 첫 제스처까지 만들지 않고, `resume()` 완료까지 기다린다.** 브라우저는 사용자가 무언가를 누르기 전에는 소리를 내주지 않는다. 페이지가 열리자마자 만든 컨텍스트는 `suspended`로 시작하고 **그 상태에서 예약한 소리는 그냥 사라진다** — 오류도 없이. `resume()`을 호출만 하고 바로 예약해도 같은 틈이 생긴다. `AudioBus.unlock()`이 실제 `running`이 될 때까지 기다린 뒤 `SoundBoard`가 첫 음악과 문 소리를 예약한다. `useAudioBoot`은 첫 `pointerdown`/`keydown`에서 이 길을 연다. 리스너는 떼지 않는다(브라우저가 컨텍스트를 다시 재우는 경우가 있다).
- **부딪힘은 `settled`가 아니라 `impacts`로 잡는다.** 자리를 잡았다는 판정은 느려진 상태가 0.35초 이어져야 나오므로, 그때 소리를 내면 화면보다 0.35초 늦다.
- **`quake`로 착지음을 낼 수 없다.** 지진은 크고 무거운 물건만 본다(`entry.shakes`). 그 조건에 걸면 대부분의 물건이 소리 없이 얹힌다. 그래서 문턱을 낮춘 `IMPACT_MIN_SPEED`로 **모든** 물건의 부딪힘을 따로 모은다. `tests/GameEvents.test.ts`가 이 회귀를 막는다.
- **무너질 때 이벤트가 폭주한다.** 한 프레임에 부딪힘이 열 개도 들어온다. 컴프레서가 진폭은 잡아주지만 "무엇이 부딪혔는지 들린다"는 되살리지 못한다 — 그건 개수의 문제라 `SoundLimiter`가 창(0.09초)마다 3개로 끊는다.
- **탭이 가려지면 컨텍스트를 통째로 재운다.** 배경음악만 멈추면 예약해둔 효과음이 돌아오는 순간 한꺼번에 터진다.
- **음악은 앞질러 예약한다.** 타이머로 그때그때 울리면 GC와 프레임에 밀려 박자가 흔들린다. 타이머는 예약만 하고 시각은 오디오 시계가 못 박는다.
- **곡을 바꿀 때 이미 예약된 음은 끊지 않는다.** 칼같이 끊으면 화면을 옮길 때마다 뚝 하고 잘린다. 남은 여운이 앞뒤를 이어주는 역할까지 한다.
- **효과음을 낮출 때 배경음악도 함께 낮춘다.** 한쪽만 내리면 균형이 뒤집혀 음악이 앞으로 나온다 — 줄이려던 것과 반대가 된다.
- **headless로는 들을 수 없다.** 확인할 수 있는 것은 컨텍스트가 열리는지·노드가 예약되는지·예외가 없는지까지다. 소리의 성질(뾰족함·균형)은 **반드시 실기로 들어봐야** 하고, 실제로 첫 판에서 그렇게 잡혔다.
  - 다만 잴 수 있는 것은 자동으로 잰다: 예약되는 음의 주파수를 모으면 화면마다 곡이 실제로 갈아끼워지는지 확인된다(`setValueAtTime`을 가로채야 한다 — `frequency.value`는 예약값이 아니라 기본값 440을 돌려준다).

## 연출

물건마다 소리가 다르고 튐이 다른데 **보이는 것은 다 같았다.** 무엇이 떨어지고 무엇이 얹혔는지가 그림 하나에만 걸려 있고, 그때 눈은 다음 단어를 쫓고 있다 — 낙하음을 재질마다 다르게 만든 것과 같은 이유로 눈 구석에서도 읽혀야 한다.

| 파일 | 하는 일 |
|---|---|
| `game/data/glowItems.ts` | 얹힐 때 화면을 물들이는 물건 15종 |
| `game/data/trails.ts` | 움직일 때 무엇을 흘리는가(갈래 표)와 얹힌 뒤 김이 오르는가 |
| `game/systems/LandingGlow.ts` | 방금 얹힌 물건의 색이 얼마나 남았는가 |
| `game/systems/TrailField.ts` | 부스러기가 어디에 얼마나 있는가. 점들의 목록 |
| `game/renderer/glow.ts` | 그 색을 얼마나 진하게 칠할지 |
| `game/renderer/trailPaint.ts` | 부스러기의 색과 진하기 |
| `game/renderer/trailShape.ts` | 부스러기의 모양 |
| `game/systems/CatPickup.ts` | 놓친 물건을 물어 가는 고양이가 지금 어디쯤인가 |
| `game/renderer/catPose.ts` | 그 고양이가 어떤 곡선으로 뛰는가 |

**어디에 무엇이 있는지는 `systems/`가, 어떻게 보일지는 `renderer/`가 안다.** 소리에서 엔진과 `audio/`를 나눈 것과 같은 경계다 — 세기와 색을 조율하는 일이 물리·시간 로직을 건드리지 않고 끝난다. `systems/` 쪽은 DOM을 모르므로 node에서 그대로 검사된다.

**연출은 판의 난수를 쓰지 않는다.** `TrailField`는 자기 난수를 굴린다 — 판의 난수열에 끼어들면 부스러기 하나 때문에 같은 시드가 같은 판을 못 만든다.

### 빛나는 물건만 화면을 물들인다

번개가 얹히면 배경이 그 색으로 옅게 번진다. 대상은 **스스로 빛을 내는 15종**이다(`glowItems.ts`) — 번개·별똥별·별가루·햇빛·달·별·촛불·손전등·탁상조명·구부러진 조명·신호등·버섯·마법봉·미러볼.

**희귀도가 아니라 빛으로 갈랐다.** 번개는 히든도 합성 전용도 아닌 평범한 단어 물건인데 화면을 물들이는 것이 자연스럽다. 드물어서가 아니라 빛나기 때문이다. 그러면 규칙이 배울 수 있는 것이 된다 — 화면이 물들면 빛나는 것이 얹혔다는 뜻이다. 금메달·반지·유리조각처럼 **반짝이기만** 하는 것은 넣지 않았다. 그 선을 풀면 밝은 색 물건이 전부 들어와 원래대로 돌아간다.

**재질 표로는 못 한다.** `spark`는 셋뿐이고 햇빛·달·미러볼은 소리가 유리에 가까워 `glass`에 앉아 있다 — 귀에는 그게 맞고 눈에는 아니다.

**밝기를 맞추고 색조만 물건에서 가져온다.** 물건 색을 그대로 쓰면 짙은 것(`#4a3a2a`)이 어두운 배경에서 아무 변화를 못 만들어, 특색이 "색이 다르다"가 아니라 "밝은 물건만 보인다"가 된다. 세기를 말하는 것은 색이 아니라 알파다.

**가산 합성(`lighter`)으로 칠한다.** 보통 합성으로 덮으면 짙은 색이 배경을 어둡게 만들어 "무엇이 얹혔다"가 아니라 "화면이 꺼졌다"로 보인다.

최대 알파 **0.085**. 배경 `#0d0f16`(r13 g15 b22) 위에서 번개가 r33 g33 b27이 된다. 0.5초 동안 제곱으로 빠진다 — 선형으로 빼면 꼬리가 남아 화면이 늘 물들어 있는 것처럼 보인다.

**무너질 때는 물들지 않는다.** `ImpactEvent.first`로 떨어져서 처음 닿은 것만 본다 — 한 프레임에 열 개가 겹치면 색이 섞여 무엇이 얹혔는지가 오히려 안 보인다. 소리는 그대로 다 난다.

### 움직이면 흘린다

물건 185종 중 **59종**만 꼬리를 갖는다(`trails.ts`). 금속·기계·유리·플라스틱은 비워뒀다 — 다 흘리면 화면이 늘 부스러기로 차서 아무 뜻이 없다.

| 갈래 | 종수 | 모양 | 성질 |
|---|---|---|---|
| `sparkle` | 15 | 네 갈래 별 | 중력 0. 뜬 자리에 남아 지나간 길을 그린다 |
| `droplet` | 17 | 늘어난 타원 | 중력 1.3. 물건보다 빨리 떨어져 뒤로 처진다 |
| `crumb` | 11 | 세 각짜리 조각 | 작고 짧게 아래로 |
| `petal` | 10 | 뾰족한 잎 | 좌우로 흔들리며 뒹군다 |
| `fluff` | 6 | 눌린 타원 | 가장 오래(2.2초) 가장 가볍게 |
| `steam` | **9** | 둥근 덩이 | **얹힌 뒤** 피어오른다. 이 표가 아니라 `STEAMING_IDS`에 산다 |
| `splash` | — | 늘어난 타원 | 닿는 순간 터진다. 물건에 배정되지 않는다 |

**모양이 가장 크게 갈라준다.** 처음에는 전부 원으로 그렸는데, 뿜는 양·수명·무게가 달라도 눈에는 "색만 다른 동그라미"였다. 잎이 흩날리는데 동그라미가 흩날리면 그것은 잎이 아니다.

**반짝임은 `glowItems.ts`와 정확히 같은 집합이다.** 같은 물건이 화면을 물들이고 또 반짝임을 흘려서 규칙이 두 번 강화된다. 표를 합치지 않은 것은 다른 갈래가 넷 더 있기 때문이다.

**기본형이 흘리면 그 단어의 히든도 흘려야 한다.** 히든은 같은 물건의 다른 형태라 맥주가 잔이든 병이든 담긴 것은 맥주다. 그런데 이 표는 물건 id로 적으므로 **아트가 올 때마다 히든 쪽을 빠뜨리기 쉽다** — 히든 24종을 붙이며 일곱이 빠졌고, 그중 넷은 액체라 **물 튀김까지 함께 사라졌다**(`burst`가 `droplet`만 보므로 표에 없으면 닿아도 아무 일이 없다). `tests/Trails.test.ts`가 지킨다. 반대 방향은 막지 않는다 — 씨앗은 아무것도 안 흘리지만 해바라기는 꽃잎을 흘리는 것이 맞다.

**색은 물건이 갖고 오되 반짝임만 밝기를 맞춘다.** 잎까지 맞추면 단풍잎도 클로버도 죄다 파스텔로 떠서 갈래 안의 특색이 사라진다.

**속도는 스냅샷의 차분으로 낸다.** `BodySnapshot`에 속도가 없는데, 자리가 매 프레임 오므로 지난 프레임과의 차이가 곧 속도다 — 연출 하나 때문에 대전 동기화가 오가는 값을 늘리지 않았다.

**정착한 물건은 흘리지 않는다**(`settled`). 안 걸면 쌓인 탑 전체가 계속 반짝인다. 점 상한은 420개다.

**뿜을 몫은 소수점째로 쌓아둔다.** 매 프레임 내림하면 느린 갈래(초당 7개)는 한 프레임 몫이 0.12라 영영 하나도 안 나온다.

**김만 조건이 반대다.** 나머지는 움직이는 동안 흘리고 정착하면 멈추는데, 김은 얹힌 **뒤에** 올라온다 — 떨어지는 동안 김이 나면 그건 김이 아니라 연기 꼬리이고, 보여주려는 것은 **쌓인 탑이 아직 살아 있다**는 것이다. 속도에 비례하지 않고 일정하게 내며, 물건 **윗면**에서 시작한다(가운데에서 내면 물건 속에서 솟는 것처럼 보인다). 중력이 음수라 뜨고 살면서 커진다 — 크기가 그대로면 위로 흐르는 점들이라 연기가 아니라 거품으로 보인다.

### 김만 갈래가 아니라 두 번째 축이다

나머지 갈래는 **한 물건에 하나**다. 김만 `TRAILS`가 아니라 `STEAMING_IDS`라는 별도 집합에 사는데, **시간이 겹치지 않기 때문**이다 — 나머지는 움직이는 동안 흘리고 김은 얹힌 뒤에 오른다. 그래서 아메리카노가 떨어지며 물방울을 흘리고, 자리를 잡은 뒤에 김을 낼 수 있다.

예전에는 김도 갈래 하나로 적었고, 그 대가가 컸다. 네 종이었는데 그중 **계란 프라이와 구멍 난 셔츠는 합성 전용이라 타이핑으로 안 떨어진다.** 실제로 스폰되는 것은 프라이팬과 다리미 둘뿐이라 판당 0.3개였고, **세 판에 두 판은 김을 아예 못 봤다.** 김은 "쌓인 탑이 아직 살아 있다"를 말하는 배경인데 그 역할을 못 하고 있었다.

축을 나누니 아홉 종이 되고 판당 0.8개가 됐다(못 보는 판 71% → 42%). **아이스 아메리카노는 넣지 않았다** — 같은 단어의 다른 형태여도 그쪽은 차갑다. 꼬리 쪽의 "기본형이 가지면 히든도 가진다"가 여기서는 통하지 않는다.

### 배경이 그림이 되면서 이펙트가 묻혔다

**배경 밝기를 재고 나서 앞선 진단을 고쳤다.** 처음에는 "가산 합성이 밝은 벽에서 더할 여지가 없다"고 봤는데 **틀렸다** — 캔버스가 투명해서 `lighter`는 **부스러기끼리 겹칠 때만** 작용하고 배경과는 나중에 일반 알파로 합성된다.

잰 값이 원인을 가리켰다(`sips`로 BMP로 바꿔 휘도를 직접 셌다).

| | 전체 | **물건이 쌓이는 자리** |
|---|---|---|
| 낮 배경 | 137 | **155** |
| 밤 배경 | 106 | **155** |

**밤에 어두워지는 것은 벽 위쪽뿐이다.** 형광등이 켜져 아래쪽은 그대로 밝다 — 부스러기와 색번짐이 실제로 놓이는 자리는 낮과 밤이 같다.

그러니 진짜 원인은 **밝기 정규화**였다. 반짝임 색이 `GLOW_LIGHTNESS = 0.6`(휘도 153)으로 맞춰지는데 배경이 155다 — **차이가 2**라 색조만 다르고 밝기가 같았다. 예전 배경(`#262b3d`, 휘도 43)에서는 110이었다.

### 곱하기는 배경 밝기와 무관하다

색번짐을 **더하기에서 곱하기로** 바꿨다. 이것이 이 절의 요점이다.

| 배경 | 방식 | 알파 | 변화 |
|---|---|---|---|
| 43 (예전 단색) | 더하기 | 0.085 | +22% |
| 155 (지금) | 더하기 | 0.085 | **0%** |
| 155 | 곱하기 | 0.22 | **−12%** |
| 43 · 106 · 137 | 곱하기 | 0.22 | **전부 −12%** |

더하기는 255가 상한이라 배경이 밝을수록 여지가 줄지만, **곱하기는 비율로 움직여 어느 배경에서도 같다.** 그래서 낮과 밤을 가르던 `GLOW_ADDITIVE_NIGHT`·`GLOW_DAY_GAIN`을 걷어냈다.

**알파에 문턱을 두지 않는다.** `tests/glow.test.ts`는 "화면이 몇 % 어두워지는가"를 지킨다(8~15%) — 합성 방식이 바뀌면 같은 알파가 다른 결과를 내므로, 알파를 못 박으면 그 문턱이 곧 거짓말이 된다.

### 밝기 상수를 둘로 나눴다

`GLOW_LIGHTNESS` 하나를 반짝임과 색번짐이 나눠 쓰고 있었는데 **둘이 원하는 방향이 반대**가 됐다.

| | 값 | 왜 |
|---|---|---|
| `SPARKLE_LIGHTNESS` | **0.88** (휘도 224) | 배경 155보다 **밝아야** 보인다 |
| `GLOW_LIGHTNESS` | **0.45** | 곱으로 덮는 색이라 **낮을수록** 진하다 |

**반짝임의 가산 합성도 걷어냈다.** 겹칠 때만 작용하는데 배경이 밝아서 하얗게 뜨기만 했다. 대비는 밝기와 테두리가 만든다.

### 예전 진단 (배경이 단색이던 때)

부스러기 값들은 **아레나 배경이 단색 어두운 색(`#0d0f16`)이던 때** 잡은 것이다. 알파 주석에도 "어두운 배경에서 이 정도는 되어야 보인다"고 적혀 있었다. 그런데 배경이 밝은 방 그림(벽·화이트보드·창문)으로 바뀌자 전제가 통째로 깨졌다.

**가산 합성이 가장 크게 망가진다.** 반짝임은 `lighter`로 칠하는데, 밝은 벽은 이미 밝아서 **더할 여지가 없다** — 낮에는 반짝임이 통째로 사라진다. 그래서 `nightfall`이 `ADDITIVE_NIGHT`(0.55) 이상일 때만 빛을 더하고, 낮에는 물감처럼 덮는다.

**형태는 테두리가 만든다.** 부스러기마다 어두운 얇은 선(1.1px)을 두른다. 물건 스티커가 검은 윤곽을 두르는 것과 같은 어법이라 겉돌지 않고, **어두운 배경에서는 배경에 묻혀 저절로 사라지므로 낮과 밤에 다른 값을 쓸 필요가 없다.** 테두리는 본체보다 늦게 옅어진다(1.15배) — 같이 옅어지면 밝은 배경에서 수명 절반쯤부터 형태가 먼저 사라진다.

**테두리는 가산 합성 밖에서 긋는다.** 빛을 더하는 상태로 어두운 선을 그으면 아무것도 더해지지 않아 선이 사라진다 — 어두운 색의 가산은 0을 더하는 것이다.

크기는 1.35배, 알파는 전반 상향했다. **김만 그대로다** — 김은 배경이라 커지거나 진해지면 쌓인 물건을 가린다.

**이 값들은 반드시 실기로 봐야 한다.** headless로 확인할 수 있는 것은 테두리가 그어지는지·낮에 가산이 꺼지는지까지다.

**김에는 따로 상한(90개)을 둔다.** 상시 뿜는 유일한 갈래라 전체 상한만 두면 뜨거운 물건이 몇 개 쌓였을 때 김이 자리를 다 차지해 **떨어지는 물건의 꼬리가 사라진다.** 김은 배경이고 꼬리는 정보다.

**불꽃 갈래는 만들지 않았다.** 물건을 전부 훑어 진짜 불은 촛불 하나뿐이고, 하나짜리 갈래는 값이 없다. 촛불은 이미 빛나는 무리에 앉아 있다. 대신 "뜨거운 것"으로 넓히니 김이 갈래가 됐다 — 아메리카노·전기주전자·붕어빵도 뜨겁지만 이미 다른 갈래를 갖고 있어 두었다. **한 물건이 갈래 둘을 갖는 구조는 아직 필요하지 않다.**

### 부딪히면 닿은 둘이 각각 반응한다

떨어진 물건만 반응하면 **받침대에 쌓인 것들은 아무 일 없는 배경**이 된다. 위에서 무언가 떨어져 나뭇잎을 쳤으면 잎이 흩날려야 하고, 물잔을 쳤으면 물이 튀어야 한다 — 그래야 쌓아둔 것이 살아 있는 것으로 보이고, **무엇 위에 얹었는지**가 부딪히는 순간 한 번 더 읽힌다.

**마른 것도 터진다.** 예전에는 액체만 튀었는데, 지금은 갈래가 있는 물건이면 **제 갈래 그대로** 터진다. 흘리는 것과 터지는 것이 같은 모양이어야 같은 물건에서 나온 것으로 읽힌다 — 잎이 흘릴 때는 잎인데 부딪히면 물방울이면 그것은 다른 물건이 된 것이다. 다만 물보다 **적게**(9개) **느리게**(0.6배) 터진다. 사방으로 흩어지는 것은 물의 성질이고 잎은 몇 장 떨어져 나올 뿐이다.

**맞은 쪽은 0.65배로 약하다.** 사건의 주인공은 떨어진 쪽이고, 같은 세기로 터지면 둘 중 무엇이 떨어진 것인지 알 수 없게 된다.

**누구를 때렸는지는 물리가 모른다.** 부딪힘을 **한 물건의 속도가 꺾인 것**으로 잡기 때문이다(`PhysicsWorld`). 접촉 쌍을 다시 물어보면 WASM 경계를 또 넘어야 하는데, 자리는 이미 매 프레임 오므로 `TrailField`가 거기서 찾는 편이 싸다 — 속도를 스냅샷 차분으로 내는 것과 같은 판단이다.

찾는 기준은 **바로 아래에서 가로로 겹치는 것 중 가장 가까운 하나**다. 옆으로 스친 것까지 세면 탑 전체가 한 번에 반응해 무엇을 쳤는지가 오히려 안 보인다. 붙었다고 보는 간격은 `CONTACT_SLACK`(0.14m)인데, 물리가 부딪힘을 잡는 시점이 **속도가 꺾인 프레임**이라 두 물건이 아직 완전히 붙어 있지 않아서다 — 0으로 두면 거의 아무도 못 찾는다.

### 물건을 놓치면 고양이가 물어 간다

목숨이 깎이는 순간은 이 게임에서 가장 중요한 사건인데, **화면에서는 가장 조용한
사건이었다** — 물건이 테두리 밖으로 날아가는 것이 전부이고 그 일은 화면 구석에서
일어나며 그때 눈은 다음 단어를 쫓고 있다. 고양이가 가로질러 오르면 그 순간이 한 번
화면 가운데를 지난다. 무엇을 잃었는지도 함께 보인다 — 그 물건을 입에 물고 간다.

**한 마리만 나온다.** 탑이 무너지면 한 프레임에 여럿이 떨어지는데, 그때마다 부르면
고양이가 여럿 교차해 무엇이 목숨을 깎았는지 오히려 안 보인다. 목숨을 깎는 이탈
하나에만 걸고, 무적 시간 중에 딸려 떨어지는 것들은 그냥 사라진다 — 엔진이 이탈을
개수가 아니라 사건으로 세는 것과 같은 기준이다.

**나올 때마다 무작위로 고른다**(네 마리). 물건에 매어 두면 같은 물건에서 늘 같은
고양이가 나와, 몇 판 만에 정보가 아니라 무늬가 된다.

**떨어진 쪽에서 들어온다.** 반대쪽에서 오면 화면을 가로질러 와야 해서 뛰어드는 것이
아니라 지나가는 것으로 보이고, 물건에 닿는 순간도 늦다. 그림 이름의 `left`/`right`는
**들어오는 쪽**이다(`jump-left`는 왼쪽에서 들어와 오른쪽을 본다).

**포물선이어야 뛴 것으로 읽힌다.** 오름은 감속하고 내림은 가속한다 — 등속으로
오르내리면 뛰는 것이 아니라 끌려 올라갔다 내려오는 것으로 보인다. 꼭대기에서 문다
(`GRAB_AT = 0.45`): 올라가며 물면 낚아챈 것이 아니라 스쳐 지나간 것이 된다.

**물기 전에는 물건이 고양이에게 붙지 않는다.** 붙여 그리면 고양이가 빈손으로
다가가는 장면이 사라져 무는 순간 자체가 없어진다. 물고 나서는 **고양이를 물건 위에
한 번 더 덮어 그린다** — 물건이 앞에 오면 얼굴을 가려 어느 고양이인지 안 보이고,
입에 문 것이 아니라 앞에 든 것으로 보인다.

**물건은 이탈하는 즉시 세계에서 치운다**(`PhysicsWorld`). 원래도 그래야 했지만
(위 밸런스 항목) 이제는 이유가 하나 늘었다 — 남겨두면 떨어지는 물건과 물려 가는
물건이 두 벌로 보인다.

**낮/밤을 가르지 않는다.** 그림이 한 장뿐이라 `drawDayNight`를 쓰지 않는다. 방의
물건이 아니라 잠깐 들어왔다 나가는 것이라 조명에 물들지 않아도 어색하지 않고,
배경에서 떨어져 나와 보이는 쪽이 이 연출이 하려는 일에 맞는다.

**배경 파이프라인(`prepare-arena.cjs`)으로 넣는다.** 고양이는 배경이 아니지만 그
파이프라인이 하는 일이 자르고 줄여 WebP로 내는 것뿐이고 고양이에게 필요한 것도
그것뿐이다 — 물건 쪽은 실루엣에서 충돌 도형을 뽑는 것이 본업인데 고양이는 부딪히지
않는다. **폭을 기준으로 크기를 맞춘다**(`CAT_WIDTH = 1.5`): 여백을 잘라내므로 네
마리의 세로가 465~590으로 제각각인데, 뛰는 자세의 가로는 곧 도약 폭이라 서로 비슷하다.

`tests/CatPickup.test.ts`가 지킨다 — 마리 수, 사라지는 시점, 판의 난수를 안 쓰는 것,
그리고 **네 마리 여덟 장이 실제로 파이프라인을 거쳐 있는지.** 마지막 것이 가장 값이
크다: `KINDS`에만 적고 등록을 잊으면 넷 중 하나가 나온 판에서만 아무것도 안 그려져서
손으로는 좀처럼 못 만난다.

### 물은 중력 반대 방향으로 튄다

액체가 담긴 물건이 닿으면 그 자리에서 **위쪽 부채꼴**로 물방울이 솟았다 되떨어진다. 부딪힘 판정은 물리 것을 그대로 쓴다(`ImpactEvent`에 `x`·`y`를 더했다) — 렌더러가 따로 판정하면 조율한 문턱이 두 벌이 되어 어긋난다.

**처음엔 방향이 틀렸다.** 반원에 고르게 뿌리고 세로 성분을 절반으로 눌렀더니 옆으로 흐르기만 해서 "튀었다"가 아니라 "번졌다"가 됐다. 위를 가운데로 두고 좌우 65도씩 펼친다(`SPLASH_FAN = 2.3`). 각도가 부채 안에 있으므로 세로 성분은 언제나 위쪽이고, 되떨어지는 것은 중력이 맡는다. **부채 가운데가 가장 높이 솟는다** — 전부 같은 속도면 폭발처럼 보인다.

**색은 담긴 것을 따르되 어긋나는 물건이 있다.** 맥주는 노랗고 딸기우유는 분홍이라 물건 색이 곧 담긴 것의 색인데, 생선(살구빛)·물뿌리개(초록)·전기주전자(빨강)는 **물건이나 통의 색**이지 물의 색이 아니다. 그대로 쓰면 생선에서 살점이 튀는 것처럼 보인다 — 그 셋만 `SPLASH_COLORS`에 물빛을 적어둔다. **김은 반대로 어느 물건에서 나든 같은 색이다**(`STEAM_COLOR`) — 남색 프라이팬에서 남색 김이 오르면 보이지도 않고, 보이더라도 물건이 번지는 것으로 읽힌다.

**세기를 크기에 그대로 곱하면 안 된다.** 액체 물건이 빈 받침대에 닿는 세기를 재보니 이랬다.

```
마티니 0.118 · 칵테일 0.259 · 아메리카노 0.383 · 우유 0.405
맥주 0.503 · 물뿌리개 0.538 · 텀블러 0.890 · 전기주전자 1.000
```

문턱이 0.12였는데 마티니가 0.118이라 **열 중 하나만 영영 안 튀었다** — 그건 문턱이 아니라 버그로 보인다(지금 0.08). 그리고 속도에 세기를 그대로 곱하니 칵테일이 초속 1m도 못 되어 5픽셀쯤 오르다 말았다. **세기는 얼마나 크게 튀는지를 정하는 것이지 튀느냐 마느냐를 정하는 것이 아니다** — `SPLASH_FLOOR = 0.55`가 바닥을 깐다.

### 밟은 함정

- **눈으로 확인하는 것이 소리보다 어렵다.** 브라우저에서 캔버스 픽셀을 세어봤더니 **세탁기 그림 하나가 5,752픽셀**이라 부스러기가 통째로 묻혔다. 가짜 2D 컨텍스트에 무엇을 그렸는지 받아 적는 쪽이 확실하다(`tests/ArenaGlow.test.ts`·`tests/Trails.test.ts`).
- **가짜 캔버스에서도 `fill()` 절대값은 못 쓴다.** 물건이 볼록 조각마다 부르므로 번개 377 대 냉장고 390처럼 물건별로 다르다. **같은 물건을 설정만 켜고 끄고** 비교해야 연출 몫만 남는다.
- **색은 흔들림 밖에서 칠한다.** 흔들림 안에 두면 색판이 함께 밀려 가장자리에 칠하지 않은 띠가 생긴다.
- **부스러기는 물건보다 뒤에 그린다.** 위에 그리면 흘린 것이 흘린 물건을 가려 꼬리를 붙인 이유와 반대가 된다.
- **렌더러에는 dt가 없다.** `update`와 따로 도는 콜백이다. 브라우저 시계를 여기서 읽으면 일시정지 중에도 부스러기가 흐르므로, 렌더 상태의 `time`(줄지 않는 초) 차이로 낸다. 싱글은 `quakePhase`를 준다 — 판이 멈춰도 흐르기 때문이다.
- **부딪힘은 dt가 0이어도 처리한다.** 물이 튀는 것은 사건이라 흘려보내면 그 프레임의 부딪힘이 통째로 사라진다.
- **끄는 길을 따로 둔다.** 흔들림·색번짐·꼬리가 각각이다. 거슬리는 방식이 서로 달라서 — 흔들림은 어지럽고 색번짐은 눈이 피로하고 꼬리는 움직이는 점이 는다. `useDisplayMenu()`가 목록을 주므로 항목을 늘려도 화면은 손대지 않는다.

## 대전에서 밟은 함정

### 재는 도구가 재려던 것을 망가뜨린다

세 번 밟았다. **대전을 실기로 확인할 때는 앱을 건드리지 말고 바깥에서만 봐야 한다.**

- `MatchEngine.onStateChange`는 **리스너를 하나만 둔다**(`this.listener = listener`). 검증 스크립트가 상태를 읽으려고 부르면 React가 붙여둔 것이 떨어져 나가고, 그때부터 화면이 갱신되지 않는다 — 결과 화면조차 뜨지 않아 "레이팅 보고가 안 나간다"로 보였다
- Playwright의 `setOffline`은 **이미 열린 WebSocket을 닫지 않는다.** 끊김을 만든 줄 알았는데 아무 일도 없었고, 그래서 재접속이 되는 것처럼 보였다
- `window.WebSocket`을 가로채면 **Vite의 HMR 소켓까지 걸려** 앱이 통째로 깨진다

끊김을 만들려면 **중계를 껐다 켜는 것**이 가장 정직하다. 페이지를 건드리지 않는다.

### dev 포트는 5173~5175만 쓴다

워커의 출처 허용 목록(`worker/src/index.ts`의 `ALLOWED_ORIGINS`)에 그 셋만 있다. 다른 포트로 띄우면 403이라 **대전도 랭킹도 통째로 안 되는데**, 화면에는 "서버에 닿지 못했습니다"만 떠서 포트 탓인 줄 모른다.

### 시험용 전송로는 실제보다 엄격하면 안 된다

`tests/helpers/hub.ts`가 두 번 거짓 실패를 만들었다. 둘 다 **실제로는 잘 도는 것이 시험에서만 깨지는** 종류다.

- 참가자 메시지를 방장에게만 보냈다 → 그건 프로토콜의 약속이지 전송로의 제약이 아니다. 실제 중계는 host를 모르고 보낸 사람만 빼고 뿌린다. 방장이 바뀌는 경우를 아예 시험할 수 없었다
- 닫힌 전송로가 계속 보냈다 → 실제 WebSocket은 닫히면 나가지 않는다. 사라진 방장이 단어 밭을 계속 뿌려 **방장이 둘인 상태**가 시험 안에서만 만들어졌다

### 폴링 주기에는 천장이 있다

큐 폴링을 늦출 때 **서버가 줄에서 치우는 기준(`QUEUE_STALE_MS` 6초)을 넘으면 안 된다.** 넘기면 멀쩡히 기다리는 사람이 줄에서 빠지고 본인은 여전히 기다리는 줄 안다 — 아끼려던 요청 대신 사람을 잃는다. 지금 4초에서 멈추고 `tests/PollDelay.test.ts`가 지킨다.

### 404를 "닿지 못했다"로 뭉개지 말 것

서버가 죽은 것과 그 기능만 없는 것은 **사람이 할 수 있는 일이 정반대다**(기다리기 vs 배포하기). 자동매칭을 처음 켠 날 워커를 배포하지 않아 404가 났는데 화면에는 "서버에 닿지 못했습니다"만 떠서 원인을 알 수 없었다. 지금은 갈라서 "서버가 아직 랭크 게임을 모릅니다"로 말하고 되풀이 요청도 멈춘다.

### 판 이름은 인원에 비례해 길어지면 안 된다

기기 id를 그대로 이어 붙였더니 여덟이 붙었을 때 306자가 되어 서버 상한에 걸렸고, 그 판의 레이팅이 통째로 버려졌다(화면에는 순위까지 다 보이는데 티어만 안 뜬다). 상한을 올리는 것은 같은 함정을 다시 놓는 일이라 FNV-1a로 접어 20자로 고정했다.

## 자동화 테스트 훅

headless 플레이 검증용으로 DOM에 노출한 것들. 프로덕션 동작에는 영향이 없다.

- `[data-word]`, `[data-state]` — 낙하 단어 칩
- `[data-lane]` — 좌/우 레인
- `[data-aim]` — 화살표 위치(-1~1). 조준을 읽는 유일한 통로

## 한 저장소를 여러 세션이 나눠 쓴다

이 저장소는 **여러 Claude 세션이 같은 폴더에서 동시에 일한다.** 그래서 git이 평소에는
문제되지 않는 자리에서 사고를 낸다 — 2026-08-09 하루에 네 건이 났고 **넷 다 같은 뿌리**다.

| 사고 | 무슨 일이 |
|---|---|
| 인덱스 쓸어담기 (2회) | `git add` 뒤 `git commit` 사이에 남이 올린 것까지 함께 커밋됐다 |
| `--amend` 덮어쓰기 | 남의 커밋을 지웠다 |
| 작업 트리가 HEAD 뒤로 처짐 | 커밋한 내용이 작업 트리에 없어, 다음 사람이 낡은 판 위에서 일했다 |

### 커밋은 **경로를 지정해서** 한다

```sh
git add <새 파일만>              # 추적 안 되던 것만
git commit -- <내 경로들>        # 인덱스에 무엇이 있든 이 경로만 들어간다
```

**`git add .` 뒤 `git commit`을 쓰지 말 것.** 인덱스가 세션끼리 공유되므로 그 사이에
남이 올린 것이 함께 나간다. 경로를 적으면 인덱스를 통째로 건너뛰므로 **잊지 않는 한**
남의 것이 섞이지 않는다.

**세션마다 인덱스를 나누는 것(`GIT_INDEX_FILE`)은 이 환경에서 못 쓴다.** 명령 사이에
환경변수가 남지 않아 매번 앞에 붙여야 하는데, 그러면 잊었을 때 조용히 원래 상태로
돌아간다 — 경로 지정과 위험은 같으면서 준비와 뒷정리가 더 든다.

### 남의 미커밋 작업이 내 파일에 얹혀 있을 때

같은 파일을 둘이 만지고 있으면 그대로 커밋할 수 없다. **HEAD 판을 꺼내 내 변경만 얹어
인덱스에 직접 넣는 방법**이 있는데(`git hash-object -w` + `git update-index --cacheinfo`),
**그때 작업 트리에도 같은 내용을 써넣어야 한다.**

빠뜨리면 커밋한 내용이 작업 트리에 없는 상태가 되고, 그 파일을 다음에 커밋하는 사람이
**조용히 되돌린다.** 실제로 `voices.ts`가 214줄 뒤처진 채 한동안 남아 있었다.

### `--amend`와 `reset`을 쓰지 않는다

되돌리는 과정이 더 위험했다. 내용이 멀쩡하면 **커밋 메시지가 틀린 채로 두는 편이 낫다** —
다음 커밋 메시지에 무슨 일이 있었는지 적으면 히스토리에서는 이어진다.

### 커밋이 경합에 밀린다 — 기다렸다 다시 한다

두 가지로 밀린다. **둘 다 정상이고 재시도가 답이다.**

| 메시지 | 언제 |
|---|---|
| `Unable to create '.git/index.lock': File exists` | 남이 `add`·`commit` 중이다 |
| `cannot lock ref 'HEAD': is at … but expected …` | 내 사전 훅이 도는 **사이에** 남이 커밋했다 |

뒤쪽이 특히 잦다. 사전 훅이 시험을 전부 돌려 **1분 가까이 걸리는데** 그동안 HEAD가
움직이면 마지막 ref 갱신이 거부된다. 검사는 통과해놓고 커밋만 실패하므로 놀라기 쉽다.

```sh
for _ in 1 2 3 4 5; do
  while [ -f .git/index.lock ]; do sleep 2; done
  git commit -F msg.txt -- <내 경로들> && break
done
```

**잠금 파일을 손으로 지우지 말 것.** 남이 실제로 쓰고 있는 중이면 그쪽 인덱스가 깨진다.
git이 남긴 안내문이 지우라고 말하지만 그건 혼자 쓸 때의 이야기다.

### 내 파일이 남의 **미커밋** 파일에 기대고 있으면 커밋될 트리가 안 돌아간다

작업 트리에서는 시험이 전부 통과하는데 커밋은 타입 오류로 막힌다. 내 새 파일이 아직
커밋되지 않은 남의 API를 쓰고 있을 때다. 하루에 두 번 겪었다 —
`ScoreManager.onWordMissed`, `ArenaBackdrop`의 `time` 프롭.

**사전 훅과 `pnpm push`가 커밋될 트리를 따로 세워 검사하므로 이건 반드시 잡힌다.**
막혔을 때 할 일은 **그쪽이 커밋할 때까지 기다리는 것**이다. HEAD의 옛 API에 맞추면
커밋은 되지만 **지금 작업 트리가 깨져** 개발 서버가 멈추고 다른 세션이 전부 막힌다.

기다리는 동안 할 수 있는 일은 있다 — **기존 파일을 건드리지 않는 새 파일**은 언제든
커밋된다. 규칙을 정하고, 순수한 부분을 새 파일로 떼어내고, 근거를 남기는 데까지는
남의 파일 없이 된다.

### 문서를 낡은 사본으로 덮는 것은 **아무도 못 잡는다**

타입도 시험도 통과하므로 훅이 그냥 지나간다. 실제로 `docs/ARCHITECTURE.md`의
`systems/` 목록에 채워둔 세 줄이 다른 커밋에 통째로 지워졌고, 알아챈 것은 한참 뒤였다.

**문서 파일이 오히려 위험하다.** 코드처럼 경로를 지정해 커밋하고, 남이 만지고 있던
문서라면 커밋 전에 `git diff`로 **내 줄만 들어가는지** 눈으로 본다.

### 내 변경이 남의 커밋에 실려 갈 수 있다

같은 파일을 둘이 만지고 있으면 그쪽이 `git add <그 파일>`을 할 때 내 변경도 함께 나간다.
내용은 멀쩡하니 사고는 아니지만, **다시 고치기 전에 HEAD에 이미 들어갔는지 본다** —
모르고 또 고치면 충돌만 는다.

```sh
git show HEAD:<경로> | grep -c '<내가 넣은 표식>'
```

## 명령어

```bash
pnpm dev
pnpm test          # vitest run
pnpm test:watch
pnpm typecheck     # tsc -b --noEmit
pnpm lint          # oxlint
pnpm lint:fix
pnpm build
node scripts/prepare-sprites.cjs
```

## 백로그

### 특별한 물건을 만나는 빈도 (보류 — 아트가 멎은 뒤에)

물건이 107종인데 **한 판에 특별한 것을 1개 보고, 열 판 중 네 판은 아무것도 못 본다**(실측은 위 밸런스 항목에). 히든 0.75 · 합성 0.27이다.

**지금 손대지 않는 이유는 아트가 계속 들어오기 때문이다.** 이 빈도는 상수 하나가 아니라 `히든 보유 단어 ÷ 전체 단어`가 정하는데, 새 물건이 올 때마다 그 분모가 움직인다. 실제로 합성 세트가 들어오며 재료 30종이 전부 단어가 되어 분모가 48 → 78로 뛰었고, 히든은 9 → 11로 둘만 늘어 비율이 19% → 14%로 **오히려 내려갔다.** 지금 `HIDDEN_CHANCE`를 맞춰봐야 다음 묶음에서 다시 어긋난다.

**아트가 올 때마다 확인할 것:** 새 물건이 재료로만 들어오면 분모만 커진다. 묶음마다 히든을 함께 늘리든지, 아니면 그만큼 확률을 올려야 제자리다.

풀 방법은 셋이고 **②가 가장 값어치가 크다고 본다.**

| | 방법 | 성격 |
|---|---|---|
| ① | `HIDDEN_CHANCE`를 올린다 | 한 줄. 즉효지만 게임이 달라지지는 않는다 |
| ② | **재료라는 것을 화면에 알린다** | `INGREDIENT_IDS`·`CRAFTABLE_IDS`가 이미 있는데 **`src` 어디서도 쓰이지 않는다.** 판이 도는 동안 플레이어는 무엇이 재료인지도 무엇과 짝인지도 모른다 — 도감을 외운 사람만 노릴 수 있다 |
| ③ | 스포너가 짝을 불러준다 | 재료가 화면·받침대에 있으면 그 짝의 스폰 확률을 올린다. 재료가 한 번도 안 갖춰진 판이 48%인 것을 직접 친다 |

②가 없으면 합성은 기능이지 플레이가 아니다. 그리고 **②가 접촉률의 의미를 바꾼다** — "갖춰져도 열에 일곱은 안 닿는다"는 값은 **중앙만 보고 떨구는 봇**으로 잰 것이고, 정보가 없는 지금은 사람도 그 봇과 다를 게 없다. 짝이 보이면 "저 옆에 붙이자"고 겨냥할 수 있고, 그때 30%는 한계가 아니라 실력 공간이 된다. Enter 하나가 조준과 제출을 겸하는 이 게임의 결에도 맞는다.

**접촉 판정을 넉넉하게 푸는 것은 마지막에 둔다.** ②③ 없이 물리부터 풀면 "안 닿았는데 합쳐졌다"가 생겨 이 프로젝트가 지켜온 전제를 깬다. ②③을 넣고 **짝 옆을 노리는 봇으로 다시 잰 뒤**에 판단할 문제다.

다시 잴 때는 `tests/zz-*.measure.test.ts` 관례를 쓴다. 둘이 필요하다 — `GameEngine`을 `FrameClock`으로 돌려 `drop`·`merge`·`gameOver` 이벤트를 세면 판 길이·히든·합성이 한 번에 나오고(60판 13초), `PhysicsWorld`를 직접 돌리며 매 프레임 `countsByVariant()`로 "재료가 살아 있는 채 갖춰졌는가"와 `findMerge`로 "합쳐졌는가"를 세면 그 차가 곧 닿지 않아 놓친 것이다(120판 51초).

### 착지 저스트 판정 (보류)

현재 콤보는 "단어를 맞추면 +1"이다. 여기에 물건이 스택에 붙는 순간 Enter를 눌러 저스트 판정을 잡는 층을 더 얹는 아이디어가 있었다. Enter는 "입력창에 글자가 있을 때"만 단어 제출에 쓰이므로, **입력창이 빈 상태의 Enter**를 착지 판정에 배정하면 키 충돌 없이 얹을 수 있다.

두 축이 Enter 하나를 두고 경쟁하는 딜레마가 생기는 게 매력이었다 — 저스트를 챙기려 손을 멈추면 단어가 바닥에 닿고, 타이핑을 계속하면 저스트를 포기한다. 다만 판정 윈도우·실패 패널티·보조 UI를 실제 플레이 감각 없이 정하기 어려워 미뤘다. `ScoreManager`는 판정 이벤트를 받을 수 있게 열려 있다.

### 대전 — 백로그가 비었다

형태는 백로그에 적어둔 (a)각자 쌓기 / (b)가비지 전송 어느 쪽도 아니게 정해졌다. **받침대 하나를 최대 8명이 함께 쓰는 턴제**다 — 한 탑을 같이 쌓으면 "상대를 방해한다"가 별도 장치 없이 규칙에서 저절로 나온다. Cloudflare Worker + Durable Object 중계를 거친다(P2P는 NAT을 못 넘어 지웠다).

여덟까지 붙고, 한 사람이 나가도 남은 사람들은 계속하며, 방장이 사라지면 다음 사람이 이어받고, 끊겼다 돌아오면 하던 판으로 복귀한다. **급한 것이 남아 있지 않다** — 다음에 볼 것은 볼트의 `04_Backlog`.

### ArenaRenderer를 월드와 연출로 가른다 (완료 — 2026-08-10)

`ArenaRenderer`는 `draw()`의 순서와 캔버스 크기·좌표 상태만 쥐고, 실제 그리기는
`arenaPaint`(월드)·`effectPaint`(연출)·`arenaArt`(에셋/낮밤)·`arenaView`(좌표 그릇)로
갈랐다. HEAD 기준 1180줄대 파일이 337줄로 줄었고, 받침대/물건/고양이 쪽과
히든/꼬리/착지 빛 쪽이 다른 파일이 됐다. `draw()`는 그대로 남겨 그리기 순서를
한 곳에서 지킨다.

1100줄이고 **하루에 커밋이 아홉 번** 들어간 날이 있었다. 큰 공유 파일은
`git commit -- <경로>` 규약으로도 안 풀린다 — 같은 파일이면 한쪽이 다른 쪽의
반쯤 된 편집을 담아 가기 때문이다.

**가르는 선은 "월드에 있는 것 / 그 위에 얹히는 것"이다.** HEAD 기준 실측이다.

| | 함수 | 줄 |
|---|---|---|
| `arenaPaint` | `drawPlatformBack`·`drawPlatformFront`·`platformRect`·`drawLedges`·`drawAim`·`drawBody`·`drawSprite`·`traceShape`·`tracePart`·`partsOf`·`drawDayNight` | **270** |
| `effectPaint` | `drawTrails`·`drawLandingGlow`·`drawHiddenReveal`·`drawGathering`·`drawFormingLedge` | **331** |
| 남는 것 | `draw`·`resize`·좌표 변환·타입·상수 | **503** |

## 파일이 작아지지는 않는다

**남는 503줄이 가장 크다.** 갈라도 `ArenaRenderer`에 절반이 남는다는 뜻이고,
그중 실제 코드는 100줄 남짓이며 나머지는 **근거를 적은 주석**이다. 그건 이 저장소가
일부러 하는 일이라 줄일 대상이 아니다.

그러니 이 작업의 값은 **"큰 파일을 쪼갠다"가 아니라 "경합을 나눈다"**이다. 아트를
만지는 쪽과 연출을 만지는 쪽이 서로 다른 파일에 있게 하는 것이 목적이다 —
실제로 그 둘이 같은 날 이 파일에서 부딪혔다. 파일 크기를 기대하면 실망한다.

**연출만 빼는 것으로는 안 된다.** 한 번 그렇게 해보려다 접었는데, 그날 부딪힌 자리가
`drawPlatform`(한쪽)과 `drawTrails`(다른 쪽)이라 연출을 빼내도 둘이 같은 파일에
남았다. 262줄을 옮기고 모듈을 넷 만들어도 겪던 문제가 하나도 안 풀린다.

## 먼저 그릇을 만든다

거의 모든 그리기 함수가 같은 것을 쓴다 — `this.scale`(17곳)·`this.toScreenY`(10곳)·
`this.toScreenX`(9곳)·`this.nightfall`. 파일을 나누려면 이것들을 넘겨줄 그릇이
있어야 한다.

```ts
interface View {
  readonly ctx: CanvasRenderingContext2D
  readonly scale: number
  readonly cameraY: number
  readonly nightfall: number
  toScreenX(worldX: number): number
  toScreenY(worldY: number): number
}
```

**이것부터 만들고 나머지는 기계로 옮긴다.** `this.`를 `view.`로 바꾸는 일이라
사람이 손으로 하면 빠뜨린다. 그릇 없이 시작하면 함수마다 인자가 제각각이 된다.

## 정할 것은 둘뿐이다

양쪽이 함께 쓰는 것은 `COLORS`·`artUrl`·`sprite()`·`LEDGE`·`ARENA` 다섯인데,
뒤의 셋은 이미 갈 곳이 있다 — `sprite()`는 `spriteCache.ts`에 있고 `LEDGE`·`ARENA`는
`config`에서 오므로 양쪽이 각자 import 하면 그만이다.

**남는 결정은 `artUrl`을 어디로 뺄지와 `COLORS`를 어디에 둘지 둘뿐이다.**

나머지 스무 개는 한쪽 전용이라 따라간다 — `UI_FONT`·`TAG_*`·`MERGE_GATHER`·
`GATHER_FROM`·`glowAlpha`·`trailPaint`는 연출로, `PLATFORM_*`·`ARROW_*`·`rim`·
`padRatio`·`PAIR_MARK_COLORS`는 월드로.

## 나눠도 순서는 안 나뉜다

`draw()`가 그리는 차례를 쥐고 있어야 한다 — "뒤 → 부스러기 → 물건 → 앞벽 →
통나무"처럼 **순서 자체가 뜻을 가지는** 자리가 있고, 그것은 전역 성질이라 파일을
나눈다고 나뉘지 않는다. `draw()`는 `ArenaRenderer`에 남긴다.

### 그 외

- ~~단어가 적어 화면이 거의 전체 어휘로 채워진다~~ → **해소됨.** 재작화 묶음에서 단어가 78 → 95개가 됐다. 동시 낙하 상한이 7이므로 이제 한 번에 보이는 것은 어휘의 7%다
- ~~사운드 없음~~ → **해소됨.** WebAudio 절차 생성. 실기로 들어보고 음량 균형을 다시 잡아야 한다 — headless로는 여기까지가 한계다
- 모바일·터치 미지원 (타자게임이므로 의도적)
- 난이도(`Difficulty.ts`)는 **시간이 아니라 탑 높이**를 따라간다. 받침대 위 `DIFFICULTY_FULL_HEIGHT`(1.6m)를 쌓으면 최대치이고, 한 번 오른 밀도는 탑이 무너져도 내려오지 않는다(`difficultyPeak`). 오르는 것은 스폰 간격(3.6→2.6초)과 동시 낙하 수(3→4)뿐이고 낙하 시간과 조준 속도는 처음부터 끝까지 같다.

  기준을 1.2m에서 1.6m로 완화했다. 1.2m는 카메라가 움직이기 시작하는 높이를 그대로 끌어 쓴 값이었는데 **재보니 그 지점이 판의 절반**이었다(봇 40판: 판 길이 44.0초 · 19.4드롭 · 최고 높이 평균 2.18m). 1.6m면 68% 지점으로 밀리고, **네 판 중 한 판은 끝까지 최대 밀도를 안 본다** — "잘 쌓는 사람에게 더 많은 단어가 오고 아직 못 쌓은 사람은 여유를 갖는다"는 규칙이 그제야 실제로 작동한다. 2.0m는 절반 가까운 판이 최대 밀도를 못 봐서 규칙이 있으나 마나 해진다. 장기 플레이 밸런스는 검증하지 않았다
