# 레시피 중심 스폰과 화이트보드

이 문서는 싱글플레이의 단어 선택과 Night Fever 정본이다. `RecipeFlow`가 어떤 레시피를 집중하는지, 화이트보드가 그 흐름과 충돌하지 않도록 어떤 순서로 갱신되는지, 밤에 레시피 재료가 어떻게 직접 떨어지는지를 함께 설명한다.

관련 코드:

- `src/game/systems/RecipeFlow.ts`
- `src/game/systems/NightFever.ts`
- `src/game/systems/Whiteboard.ts`
- `src/game/systems/WordSpawner.ts`
- `src/game/core/GameEngine.ts`
- `tests/RecipeFlow.test.ts`
- `tests/NightFever.test.ts`
- `tests/Whiteboard.test.ts`
- `tests/Recall.test.ts`
- `tests/zz-recipe-flow.measure.test.ts`

## 지켜야 하는 규칙

1. **집중 레시피를 화이트보드보다 먼저 정한다.**
2. **현재 집중 레시피에서 타이핑으로 낼 수 있는 재료 단어는 화이트보드 후보에서 제외한다.**
3. 낮과 밤에는 107개 단어를 모두 열어 두되, RecipeFlow가 레시피 재료의 간격을 조절한다.
4. 밤에는 여섯 개를 0.3초 간격(총 1.8초)으로 내리고 3초 쉰다. 밤 안에 두 묶음, 총 12개다.
5. 각 묶음은 상단에 쌓인 물건을 완성하는 나머지 2재료 레시피 재료를 우선하고, 빈칸은 완성 가능한 레시피 쌍으로 채운다.
6. 묶음 안 물건은 0.3초 간격이며, x는 표적 주변에서 매번 다르게 흔든다.
7. Fever 물건은 낙하 중 연보라 별똥별 꼬리를 남긴다.
8. Night Fever의 예정 재료도 RecipeFlow의 현재 재료 개수에 포함한다.
9. 화이트보드 우선 스폰이 끼어들어도 RecipeFlow의 집중 레시피와 제안 횟수는 진행시키지 않는다.
10. 같은 시드와 같은 관측 상태는 같은 RecipeFlow·Fever 순서를 만든다.
11. 이 흐름은 싱글플레이에만 적용한다. 대전은 기존 랜덤 스폰을 유지한다.

핵심 순서는 다음과 같다.

```text
현재 물건 관찰
→ 집중 레시피 확정
→ 집중 재료를 제외하고 화이트보드 보충
→ 화이트보드 추가 가중치 추첨
→ 미당첨이면 RecipeFlow가 단어 선택
→ 레인에 스폰
```

## 모듈의 책임

### RecipeFlow

현재 집중할 레시피와 그 레시피에서 더 필요한 재료를 정한다.

- 레시피 44개를 직접 레시피 34개와 연쇄 레시피 10개로 분류한다.
- 직접 레시피는 기본 단어만으로 완성할 수 있다.
- 연쇄 레시피는 다른 합성 결과물이 판에 있어야 집중 후보가 된다.
- 직접 레시피의 재료가 아닌 단어는 ambient 무리에 둔다.
- 현재 재료 개수는 외부에서 주입받고 DOM, canvas, Rapier를 직접 알지 않는다.

### NightFever

밤에 여섯 개를 1.8초에 걸쳐 내리고 3초 쉬는 묶음을 만들며 낙하 시각과 x를 정한다.

- 정착한 물건의 윗면 중 최고점에서 1.2m 안을 "상단"으로 본다.
- 상단 물건이 2재료 레시피의 한쪽이면 나머지 한쪽을 표적 주변에 내린다.
- 같은 표적은 묶음마다 한 번만 쓴다. 첫 합성 뒤 남은 재료가 보장 없는 잉여가 되는 것을 막기 위해서다.
- 상단 표적 수는 짝수로 고르고, 남는 칸은 두 재료를 모두 내리는 완성 레시피 쌍으로 채운다.
- 완성 후보가 없으면 무작위 2재료 레시피 세 쌍을 통째로 내린다.
- 묶음은 밤 0초와 4.8초에 열린다. 여섯 개를 0.3초 간격으로 1.8초 동안 내린 뒤 3초 쉰다.
- 다음 9.6초 묶음은 밤 안에 여섯 개를 다 내릴 수 없어 열지 않는다.
- Fever 시계는 입력 드롭의 0.3초 물리 쿨다운과 독립적으로 흐른다.
- x는 물건이 받침대 안에 남는 범위에서 표적 주변 또는 레시피별 무작위 기준점으로 흔든다.
- RecipeFlow·단어·히든과 분리한 전용 RNG를 쓴다.

### Whiteboard

현재 보드에 적힌 최대 3개의 회수 단어를 관리한다.

- 주어진 단어 밭 안에서만 채운다.
- 이미 유효한 보드 항목은 유지한다.
- 밭을 벗어났거나 `excluded`에 포함된 항목만 교체한다.
- 회수한 단어는 한 칸을 비운 뒤 같은 밭과 제외 목록으로 즉시 보충한다.
- 어떤 단어가 레시피 재료인지는 알지 않는다. 제외 목록은 GameEngine이 넘긴다.

### WordSpawner

좌우 레인, 중복 방지, 스폰 간격과 최종 선택 순서를 관리한다.

싱글의 선택 우선순위는 다음과 같다.

```text
화이트보드 추가 가중치
→ RecipeFlow.pick()
→ 최후의 기본 랜덤
```

대전에는 RecipeFlow 선택기를 주입하지 않으므로 기존 랜덤 선택을 유지한다.

### GameEngine

세 시스템을 시간순으로 조립한다.

- 물리 세계, 활성 단어, 드롭 대기열, Fever 예정 재료를 세어 RecipeFlow에 전달한다.
- RecipeFlow가 집중 레시피를 먼저 확정하게 한다.
- 집중 재료 단어를 Whiteboard의 제외 목록으로 넘긴다.
- Whiteboard의 현재 단어를 WordSpawner의 선호 목록으로 넘긴다.
- 회수 드롭은 RecipeFlow가 보는 재료 개수에서 제외한다.
- 밤 진입과 새벽 전환에 NightFever를 시작·정리한다.
- 밤 이탈은 목숨과 콤보를 깎지 않고, 새벽에는 기존 2초 보호막을 잇는다.

## RecipeFlow의 분류와 선택

### 직접·연쇄·ambient

| 무리 | 현재 구성 | 의미 |
|---|---:|---|
| 직접 레시피 | 34개 | 모든 재료가 기본 단어로 존재한다 |
| 연쇄 레시피 | 10개 | 입력 중 하나 이상이 다른 합성 결과물이다 |
| 직접 재료 | 65종 | 직접 레시피가 집중할 수 있는 기본 단어 |
| ambient | 42종 | 비레시피 28종 + 연쇄 레시피에서만 쓰이는 기본 재료 14종 |

ambient에 연쇄 레시피의 기본 재료도 넣는 이유는 선행 합성 결과물이 없을 때도 모든 단어가 계속 등장해야 하기 때문이다.

### 국면별 밀도

| 국면 | 길이 | RecipeFlow 선택 | 직접 낙하·보호 |
|---|---:|---|---|
| 낮 | 20초 | 레시피 선택 2회마다 ambient 1회 | 없음 |
| Night Fever | 10초 | 레시피 선택 3회마다 ambient 1회 | 6개 묶음 2회(총 12개)·하트 무적 |

국면은 단어 낙하 속도와 밀도를 바꾸지 않는다. 밤은 RecipeFlow의 ambient 간격을 늘리는 동시에 NightFever가 0.3초 간격의 물건 묶음을 직접 내린다.

### 레시피 순서

낮과 밤에는 현재 물건을 활용해 완성에 가까운 레시피를 먼저 찾는다. 입력 다중집합 중 이미 갖춘 비율이 가장 높은 레시피가 우선이며, 선행 합성 결과물을 갖춘 연쇄 레시피도 같은 후보에 들어간다.

완성 가능 레시피만 연속하면 같은 재료가 자주 보여 어색하므로 다음 패턴을 지킨다.

```text
완성 가능성이 높은 레시피 1개
→ 셔플백의 새 레시피 1개
→ 셔플백의 새 레시피 1개
→ 완성 가능성이 높은 레시피 1개
```

두 새 레시피는 완성 후보에서 제외하고, 가능하면 직전 두 레시피와 입력 재료도 겹치지 않는 것을 고른다. 선택지가 부족할 때만 재료 중복을 허용한다. 완성 가능한 후보가 없으면 새 레시피를 고르고, 이후 후보가 생기는 시점에 다시 위 패턴을 시작한다.

### 집중 레시피의 수명

집중 레시피는 다음 중 하나가 일어날 때 바뀐다.

- 관찰된 재료 개수가 이미 입력을 모두 충족했다.
- 입력으로 낼 수 있는 단어 수에 추가 여유 2회를 더한 제안 상한에 닿았다.
- 실제 합성이 성공해 `onMerged()`가 호출됐다.
- 낮·밤 국면이 바뀌었다.

히든이나 이탈로 재료가 사라져도 추가 제안 두 번까지만 허용하는 이유는 한 레시피에 영원히 갇히지 않기 위해서다.

### 현재 재료로 세는 것

GameEngine은 스폰 직전에 다음을 합쳐 `variant id → 개수`로 전달한다.

- 물리 세계에 남아 있는 물건
- 현재 레인에서 내려오는 활성 단어의 기본 물건
- 아직 떨어지지 않은 일반 드롭 대기열
- 이번 Night Fever에서 아직 떨어지지 않은 예정 재료

화이트보드 회수 드롭은 곧 사라지므로 개수에 넣지 않는다. 활성 단어는 아직 회수 여부가 확정되지 않았으므로 일단 센다. Fever 예정 재료를 미리 세야 RecipeFlow가 같은 재료를 불필요하게 다시 집중하지 않는다.

## 화이트보드와 결합되는 시간순 흐름

### 1. 판 시작

GameEngine은 낮에서 시작하며 첫 상태를 내보내기 전에 다음 순서를 지킨다.

```text
phase = day
→ 현재 물건·활성 단어·대기열 관찰
→ RecipeFlow.prepareFocusWords()
→ 집중 레시피와 재료 단어 확정
→ Whiteboard.refill(WORDS, focusedWords)
→ WordSpawner.prefer(whiteboard.words)
```

화이트보드는 처음부터 집중 레시피 입력을 뺀 나머지 단어에서 세 칸을 채운다. 따라서 첫 화이트보드가 집중 레시피보다 먼저 정해지는 순간은 없다.

### 2. 매 스폰 직전

`prepareFocusWords()`는 현재 집중 레시피가 유효하면 같은 단어 목록을 돌려준다. 레시피가 끝났다면 다음 레시피를 정한 뒤 새 목록을 돌려준다.

GameEngine은 매 프레임 이 목록으로 Whiteboard를 다시 맞춘다.

- 기존 보드 단어가 새 집중 레시피 재료가 되면 즉시 보드에서 빠진다.
- 나머지 유효한 보드 단어는 유지한다.
- 빠진 칸만 새 단어로 채운다.
- 이미 레인에 내려오는 단어라도 새 집중 재료가 되면 회수 표식에서 빠져 일반 드롭으로 돌아간다.

### 3. WordSpawner의 화이트보드 추첨

화이트보드 후보를 `P`, 전체 스폰 후보를 `N`이라고 하면 추가 가중치 몫은 다음과 같다.

```text
extra = P × (4 - 1)
화이트보드 강제 선택 확률 = extra / (N + extra)
```

화이트보드가 3개이고 화면에 활성 단어가 없다면 `9 / 116`, 약 7.8%다. 이 추첨이 실패하면 RecipeFlow가 현재 레시피의 부족한 재료 또는 ambient를 고른다. RecipeFlow가 우연히 화이트보드 단어를 고를 수도 있으므로 싱글의 최종 화이트보드 출현 확률은 고정 4배가 아니다.

화이트보드가 강제로 고른 단어는 RecipeFlow의 다음 값을 바꾸지 않는다.

- 현재 집중 레시피
- 집중 레시피 제안 횟수
- recipe-to-ambient 간격
- RecipeFlow의 마지막 선택 단어

따라서 화이트보드 단어는 집중 흐름 사이에 추가로 끼어드는 단어다.

```text
레시피 재료
→ 화이트보드 단어
→ 레시피 재료
→ RecipeFlow ambient
```

현재 집중 재료는 보드 후보에서 제외되므로 화이트보드가 그 재료를 회수해 합성을 끊지는 않는다. 다만 다른 레시피의 재료는 보드에 들어갈 수 있다.

### 4. 단어를 쳤을 때

#### 보드에 없는 단어

- 조준 위치에 일반 드롭으로 들어간다.
- 드롭 대기열부터 RecipeFlow의 현재 재료 개수에 포함된다.
- 화이트보드는 바뀌지 않는다.

#### 보드에 있는 단어

- 보드에서 해당 단어를 제거한다.
- **같은 집중 레시피 제외 목록**으로 빈 칸을 즉시 보충한다.
- 레인 쪽 회수 판으로 떨어뜨린다.
- 목숨을 깎지 않고 물리 세계에서 제거한다.
- 회수 드롭은 RecipeFlow 재료 개수에 포함하지 않는다.

회수한 단어를 새 후보에서 별도로 금지하지는 않으므로, 집중 재료가 아니라면 낮은 확률로 같은 단어가 다시 보드에 들어갈 수 있다.

### 5. 단어를 놓쳤을 때

놓친 보드 단어는 보드에서 제거되지 않는다. 물건도 생성되지 않고 콤보만 끊긴다. 활성 단어가 화면에서 사라지면 다음 스폰부터 다시 화이트보드 우선 후보가 될 수 있다.

### 6. 낮과 밤 전환

국면이 바뀌면 RecipeFlow는 진행 중인 집중 레시피와 제안 횟수를 초기화한다. 같은 프레임에 새 국면의 레시피를 먼저 확정하고 화이트보드를 다시 맞춘다.

밤에 들어가면 NightFever가 1.8초 낙하·3초 휴식 시계를 연다. 묶음을 시작할 때마다 그 프레임의 상단 스택을 다시 읽으므로 앞선 합성 결과도 다음 표적이 될 수 있다. 밤 동안 모든 이탈은 물건만 제거하고 목숨·콤보·고양이·붕괴를 건너뛴다. 새벽에는 남은 Fever 낙하를 버리고 `INVULNERABLE_SEC` 2초 보호막을 시작한다.

낮과 밤 모두 전체 107개 단어를 쓰므로 국면 전환만으로 보드를 통째로 갈지 않는다. 새 집중 레시피와 겹치는 항목만 교체한다.

## 화이트보드 표시 전환

보드 이미지는 원래 배치 폭의 90%로 그리되 글자 크기는 줄이지 않는다. 글자는 중앙 안전 영역에 배치한다. 목록이 바뀌어도 유지되는 단어는 움직이지 않고, 실제로 빠진 단어와 새로 들어온 단어만 `whiteboardWordChanges()`가 가른다.

- 판이 열릴 때 세 단어를 60ms 간격으로 왼쪽부터 써 내려가듯 드러낸다.
- 단어가 교체되면 이전 글자를 380ms 동안 지우개 자국과 함께 지운다.
- 지우기 시작 300ms 뒤 새 글자를 460ms 동안 왼쪽에서 오른쪽으로 써 내려가듯 드러낸다.
- 연필이나 지우개 물체는 그리지 않고 글자, 번짐, 닦인 자국만 쓴다.
- `prefers-reduced-motion: reduce`에서는 지우기 잔상을 숨기고 새 글자를 즉시 표시한다.

애니메이션 중에도 게임 규칙의 `Whiteboard.words`는 이미 새 목록이다. 연출은 규칙을 늦추지 않으며, 교체가 드문 상태 설명이므로 짧은 지우기·쓰기 전환만 허용한다.

## 난수와 결정론

- RecipeFlow는 판 시드에서 분리한 전용 RNG를 쓴다.
- NightFever도 레시피·x 선택에 별도 RNG를 쓴다.
- Whiteboard도 별도 RNG를 쓴다. 현재 GameEngine의 Whiteboard 시드는 `0x5eed`다.
- 화이트보드 추가 가중치 추첨은 WordSpawner의 판 RNG를 쓴다.

따라서 Fever 물건이나 화이트보드 목록을 뽑는 행위는 RecipeFlow·단어·히든 난수열을 밀지 않는다. 다만 화이트보드 우선 추첨이 실제로 실행되면 WordSpawner와 히든 판정이 공유하는 판 RNG는 소비된다. 전체 실행은 결정론적이지만 화이트보드가 없는 실행과 판 난수열까지 같지는 않다.

## 변경할 때 지킬 계약

- `RecipeFlow.pick()` 안에서 처음 집중 레시피를 정하도록 되돌리지 않는다. 화이트보드가 먼저 채워지는 회귀가 생긴다.
- 화이트보드를 `WORDS` 전체로 먼저 채운 뒤 나중에 시각적으로만 숨기지 않는다. `Whiteboard.words`와 `WordSpawner.preferred` 자체에서 집중 재료가 빠져야 한다.
- 보드 회수 후 보충할 때도 동일한 집중 재료 제외 목록을 넘긴다.
- 집중 레시피가 바뀌면 기존 보드 항목 중 겹치는 것만 교체하고 나머지는 유지한다.
- 완성 가능 레시피를 더 자주 호출하더라도 그 사이의 새 레시피 두 개와 재료 비중복 우선을 제거하지 않는다.
- 화이트보드 강제 선택을 RecipeFlow의 recipe/ambient 카운터에 포함하려면 먼저 밀도 측정 기준을 다시 정한다.
- Fever x에 `AIM_HALF_RANGE`를 쓰지 않는다. 그 범위는 조준 실패를 허용하려고 받침대 밖까지 열려 있다.
- 같은 상단 물건을 한 묶음에서 두 번 표적으로 삼지 않는다. 첫 재료가 합친 뒤 나머지는 더 이상 보장된 조합이 아니다.
- Fever 낙하에 입력용 `DROP_COOLDOWN_MS`를 적용하지 않는다. 0.2초 리듬이 0.3초 문턱에서 무너진다.
- Fever 예정 재료를 RecipeFlow 관측에서 빼지 않는다. 같은 재료를 단어로 다시 부르는 회귀가 생긴다.
- Fever 표식은 물리 결과를 바꾸지 않고 렌더 스냅샷까지만 전달한다. `TrailField`가 이 표식을 연보라 `sparkle` 꼬리로 바꾼다.
- 자동 물건도 정착 점수·도감·합성 보너스를 기존 물건과 똑같이 받는다. 밤에 놓친 단어는 정확도 점수에서 제외해 Fever 동안 화면 점수가 내려가지 않고, 양수 점수 델타는 연보라색으로 표시한다. 다만 Fever 중 새 방어용 먼지구름·통나무는 만들지 않는다.
- 낮 합성이 연달아 일어나면 통나무 보상 횟수를 대기열에 쌓는다. 이미 낮에 시작한 먼지구름은 밤이 되어도 그대로 완성된다.
- 대전에 적용하려면 RecipeFlow와 NightFever 상태를 방장 교체 때 동기화해야 한다.

## 직전 30개 규칙의 짧은 실측

아래 값은 **밤당 30개였던 직전 규칙**을 `2026-08-10`에 3판·60초로 돌린 결과다. 현재 12개 규칙의 값이 아니다. 장기 밸런스가 아니라 낙하 수와 합성 경로가 실제 엔진에서도 열리는지 보는 연기 시험이다.

| 지표 | 값 |
|---|---:|
| 60초 상한 도달 | 3/3판 |
| 전체 드롭 | 82.7개 (입력 22.7 · Fever 60.0) |
| 실제 합성 | 21.33회 · 못 한 판 0% |
| 합성 밀도 | 100드롭당 25.81회 · 분당 21.33회 |
| 3드롭 안의 2재료 조합 | 4.33회 |
| 8드롭 안의 3개 이상 조합 | 3.67회 |

두 번의 밤에서 정확히 60개가 내려왔고 3판 모두 60초에 살아 있었다. 요청한 폭우와 합성 보장은 확인됐지만 장기 플레이는 더 강해졌다. 180초 10판 재측정 전에는 이 값을 최종 밸런스로 읽지 않는다.

## 직전 Fever 실측

아래 값은 **밤당 6개를 내리던 이전 규칙**의 `2026-08-10` 측정이다. 현재 12개 규칙의 최신 밸런스가 아니며 비교 기준으로만 남긴다. 자동 낙하도 실제 물건이므로 전체 드롭과 합성 밀도에 포함한다.

| 지표 | Fever 전 | **Fever 후** |
|---|---:|---:|
| 판 길이 | 114.0초 | **166.9초** |
| 전체 드롭 | 43.0 | **97.3** (입력 64.3 · Fever 33.0) |
| 고유 단어 | 34.6 | **54.0** |
| 비레시피 물건 | 7.40개 · 전체의 17% | 12.20개 · **입력 드롭의 19%** |
| 3드롭 안의 2재료 조합 | 11.10회 | **16.30회** |
| 8드롭 안의 3개 이상 조합 | 1.20회 | **1.70회** |
| 실제 합성 | 3.10회 · 못 한 판 10% | **19.60회 · 못 한 판 0%** |
| 합성 밀도 | 100드롭당 7.21회 · 분당 1.63회 | **100드롭당 20.14회 · 분당 7.05회** |
| 180초 측정 상한 도달 | — | **90%** |

목표였던 "밤에는 합성이 쉬워진다"는 분명하게 나타났다. 반면 10판 중 9판이 180초 안에 끝나지 않았다. 밤 무적, 안전 x의 무료 재료, 합성 통나무가 함께 판을 크게 늘린 결과다. 이 측정은 회귀 기준이면서 장기 밸런스 리스크다. 세 효과를 나누어 다시 조정하지 않은 채 수치 하나만 낮추면 무엇이 판을 늘렸는지 알 수 없으므로, 다음 조정에서는 Fever 묶음 수·새벽 보호·통나무 기여를 각각 재야 한다.

## 검증

빠른 계약 검증:

```zsh
pnpm exec vitest run tests/DayNight.test.ts tests/NightFever.test.ts tests/RecipeFlow.test.ts tests/Whiteboard.test.ts tests/Recall.test.ts tests/Vitals.test.ts
```

전체 검증:

```zsh
pnpm test
pnpm typecheck
pnpm lint
pnpm build
```

밸런스 측정:

```zsh
MEASURE=1 pnpm exec vitest run tests/zz-recipe-flow.measure.test.ts --disableConsoleIntercept
```

측정 테스트는 고정 물리 스텝 비용 때문에 기본 10판·판당 180초 상한을 사용한다. 레시피, 물건, 물리, 히든 확률, 화이트보드 우선 규칙, Fever 묶음 수나 보호 시간이 바뀌면 다시 실행한다.
