import { chromium } from 'playwright-core'
import assert from 'node:assert/strict'
const browser=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});const page=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text())});
try {await page.goto('http://127.0.0.1:5183/?three-prototype=1');await page.waitForSelector('[data-ready="true"]');await page.getByRole('button',{name:'싱글 새로 시작',exact:true}).click();await page.waitForTimeout(1800);await page.screenshot({path:'artifacts/three-prototype/window-day.png'});console.log(await page.locator('[data-renderer]').evaluate(e=>({...e.dataset})));await page.locator('summary').click();await page.getByLabel('밤 조명').check();await page.locator('summary').click();await page.waitForTimeout(500);await page.screenshot({path:'artifacts/three-prototype/window-night.png'});await page.setViewportSize({width:390,height:844});await page.waitForTimeout(300);await page.screenshot({path:'artifacts/three-prototype/window-mobile.png'});assert.equal(await page.locator('[data-window-ray]').count(),0);assert.equal(await page.locator('[data-renderer]').getAttribute('data-window-night'),'1');
await page.setViewportSize({width:1440,height:1000});await page.locator('summary').click();await page.getByRole('button',{name:'기존 2D',exact:true}).click();await page.waitForTimeout(200);assert.ok(await page.locator('[data-window-ray]').count()>0);
await page.getByRole('button',{name:'2D 물건 + 3D 상자',exact:true}).click();await page.waitForSelector('[data-ready="true"]');assert.equal(await page.locator('[data-window-ray]').count(),0);assert.deepEqual(errors,[]);console.log('Window light: day/night, resize, 2D fallback and WebGL console passed');}finally{await browser.close()}
