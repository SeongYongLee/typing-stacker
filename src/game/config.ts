/**
 * 아레나 좌표계: 원점은 받침대 중앙, x는 오른쪽 +, y는 위쪽 + (단위: 미터).
 * Rapier는 수 미터 규모에서 가장 안정적이라 픽셀이 아니라 미터로 시뮬레이션하고,
 * 렌더러가 px로 환산한다.
 */
const ARENA = {
  /*
   * 점선 틀의 반폭. 받침대 밖으로 이만큼 여유가 있다.
   *
   * 넓히면 굴러떨어지는 물건이 끝까지 보이고 조준할 여지도 넓어 보인다. 대신
   * 월드가 커지므로 같은 캔버스에서는 모든 것이 작아진다 — 그래서 아레나를 그리는
   * 화면 폭(ARENA_SCREEN_MAX_WIDTH)을 같은 비율로 함께 키워 받침대 크기를 지킨다.
   * 둘 중 하나만 바꾸면 물건이 커지거나 작아진다.
   */
  halfWidth: 3.2,
  height: 5.2,
  /** 받침대 윗면 높이 */
  platformTop: 0.8,
  /**
   * 받침대 반폭.
   * 양옆에 벽이 없으니 미끄러진 물건을 받아줄 것은 받침대 폭뿐이다.
   * 좁게 잡으면 한 칸 폭 탑을 쌓다가 살짝 미끄러지는 것만으로 즉사해서,
   * 실력과 무관하게 두세 개에서 끝난다.
   *
   * 2.0은 너무 관대해서 미끄러져도 대개 받침대가 받아냈다. 조준 범위가 이 값에서
   * 파생되므로(AIM_HALF_RANGE) 좁히면 조준도 같이 좁아진다 — 한 번에 크게 줄이지 않는다.
   */
  platformHalfWidth: 1.85,
  platformHalfHeight: 0.25,
  /**
   * 물건이 생성되는 높이. 카메라가 올라가면 이만큼 위에서 계속 떨어진다.
   * 탑이 이 높이에 닿으면 새 물건이 탑 속에 생겨 서로를 밀어내므로,
   * 카메라 없이는 여기가 곧 게임의 천장이었다.
   */
  spawnY: 4.6,
  /** 이 높이보다 아래로 내려간 물건은 이탈로 본다 */
  killY: -0.8,
  /**
   * 지구 중력(-9.81)보다 약하게 잡았다.
   * 스폰 높이에서 받침대까지는 3m가 넘는데, 실제 중력이면 착지 속도가 8m/s를 넘어
   * 이미 쌓아둔 물건을 밀어내고 스택이 두세 개에서 무너진다.
   * 약한 중력은 낙하가 눈으로 읽히게 만들고 조준할 시간도 준다.
   */
  gravity: -7,
} as const

/**
 * 탑 꼭대기 위로 남겨두는 여유. 이보다 좁아지면 카메라가 따라 올라간다.
 *
 * 조준 화살표와 낙하 구간이 들어갈 만큼은 되어야 한다 — 붙자마자 닿을 만큼
 * 좁으면 무엇을 어디에 떨구는지 볼 시간이 없다.
 */
const CAMERA_HEADROOM = 2.6

/**
 * 카메라가 목표 높이를 따라가는 빠르기(초당 비율).
 * 즉시 옮기면 물건 하나 얹을 때마다 화면이 툭툭 튀어 어디에 떨어지는지 놓친다.
 */
const CAMERA_FOLLOW = 3.5

/** 가장 큰 물건(비행기)의 반폭. tests/shapes.test.ts가 이 값을 지킨다 */
const MAX_ITEM_HALF_WIDTH = 0.55

/**
 * 화살표가 훑는 범위. 받침대 반폭과 같다.
 *
 * 예전에는 가장 큰 물건의 반폭을 빼서, 어느 타이밍에 Enter를 쳐도 물건이 받침대에
 * 온전히 얹히게 했다. 조준 실수로 즉사하지 않게 하려는 안전장치였는데 대가가 있었다 —
 * 조준이 틀려도 손해가 없으니 이 게임의 핵심 긴장("빨리" 대 "정확히")에서 정확히
 * 쪽의 보상이 사라졌다. 실제로 봇으로 일부러 가장자리만 노려도 판이 끝나지 않았다.
 *
 * 이제 받침대 끝까지 조준할 수 있다. 끝을 노리면 물건이 절반쯤 걸치므로 Enter를
 * 언제 누르는지가 곧바로 값을 치른다.
 */
/**
 * 받침대 밖으로 조준할 수 있는 여유.
 *
 * 끝을 노리면 물건 중심이 받침대를 벗어나므로, 작은 물건은 얹히지 못하고 그대로
 * 떨어진다. 조준이 실제로 값을 치르게 하려는 것이다 — 받침대 안에서만 조준하면
 * 어디를 노리든 일단 얹히고, "정확히"의 대가가 늦게 흐릿하게만 돌아온다.
 */
const AIM_OVERHANG = 0.25

const AIM_HALF_RANGE = ARENA.platformHalfWidth + AIM_OVERHANG

/**
 * 대전에서 판이 열리기 전에 세는 초.
 *
 * 누르는 순간 바로 시작하면 첫 단어가 이미 내려오고 있다 — 누른 사람은 마우스에
 * 손이 가 있고 자판으로 옮길 틈이 없다. 대전에는 이유가 하나 더 있다: 마지막에
 * 준비를 누른 사람이 아니면 언제 열리는지 모른 채 당한다. **남은 시간을 알려주는
 * 것 자체가 일**이라 숫자를 센다.
 *
 * 혼자 할 때는 그 두 번째 이유가 없어서 세지 않는다 — 아래 `SOLO_*`를 볼 것.
 */
const COUNTDOWN_SEC = 3

/**
 * 혼자 하기는 숫자를 세지 않고 **READY → START** 두 박자로 연다.
 *
 * 대전에서 숫자를 세는 이유는 손을 옮길 틈 말고 하나가 더 있다 — 마지막에 준비를
 * 누른 사람이 아니면 언제 열리는지 모른 채 당한다. 혼자 할 때는 방금 자기가 눌렀으니
 * 그 이유가 없고, 남는 것은 손을 옮길 틈뿐이다. 그 틈에 셋을 세면 다시 하기를
 * 되풀이할 때 길게 느껴진다.
 *
 * READY가 길고 START가 짧다. 손을 부르는 데는 시간이 걸리지만 놓아주는 것은
 * 신호 하나면 된다 — 길이가 같으면 둘 다 대기로 읽힌다.
 */
const SOLO_READY_MS = 750
const SOLO_START_MS = 450

/**
 * 받침대 위로 이만큼 쌓으면 단어 밀도가 최대치에 닿는다(`Difficulty`).
 *
 * 예전에는 이 값을 따로 두지 않고 **카메라가 움직이기 시작하는 높이**에서 끌어 썼다
 * (`CAMERA_START_TOP - platformTop` = 1.2m). "판이 본격적으로 시작됐다는 것을 눈으로
 * 아는 순간"과 밀도가 오르는 시점을 맞추려던 것이었는데, 재보니 **그 지점이 판의
 * 절반**이었다. 나머지 절반을 내내 최대 밀도로 보내는 셈이다.
 *
 * 봇 40판 실측(판 길이 44.0초 · 19.4드롭 · 최고 높이 평균 2.18m):
 *
 * | 기준 | 최대 밀도까지 | 판의 몇 % 지점 | 끝까지 못 닿은 판 |
 * |---|---|---|---|
 * | 1.2m (예전) | 22.6초 · 8.8드롭 | **52%** | 3/40 |
 * | **1.6m (지금)** | 31.2초 · 13.2드롭 | **68%** | 10/40 |
 * | 2.0m | 37.7초 · 16.5드롭 | 78% | 18/40 |
 *
 * **못 닿는 판이 생기는 것은 흠이 아니라 뜻한 바다.** 난이도가 높이를 따라가는 이유가
 * "잘 쌓는 사람에게 더 많은 단어가 오고 아직 못 쌓은 사람은 여유를 갖는다"인데,
 * 1.2m에서는 거의 모든 판이 최대치를 봤으니 그 규칙이 사실상 꺼져 있었다. 1.6m면
 * 네 판 중 한 판은 끝까지 여유롭게 간다.
 *
 * 2.0m까지 밀지 않은 이유는 절반 가까운 판이 최대 밀도를 아예 못 보기 때문이다.
 * 그쯤 되면 밀도가 오르는 규칙 자체가 있으나 마나 하다.
 *
 * 카메라와의 연결이 끊어진 것은 알고 한 것이다. 카메라가 움직이는 순간은 여전히
 * "이제 시작"의 신호이고, 밀도는 그 뒤로 조금 더 오른다.
 */
const DIFFICULTY_FULL_HEIGHT = 1.6

/**
 * **합성했을 때** 공중에 놓이는 작은 통나무.
 *
 * 판을 끝내는 것은 점수가 아니라 얹을 자리가 좁아지는 것이니, 자리를 하나 더 주는
 * 것이 이 게임의 말로 된 보상이다. 규칙은 `systems/Ledge.ts`에, 왜 합성에만
 * 주는지는 `GameEngine.growLedge`에.
 *
 * **폭은 가장 큰 물건이 온전히 얹히는 값이다.** `MAX_ITEM_HALF_WIDTH`(0.55)보다
 * 좁으면 비행기·자전거가 양쪽으로 삐져나와 놓자마자 굴러떨어진다 — 새 자리를 줬는데
 * 쓸 수 있는 물건이 따로 있는 셈이 된다.
 *
 * 받침대 그림(`stack-platform-log.png`)을 줄여서 쓴다. 새 아트가 필요 없고,
 * 무엇보다 **같은 그림이라 "받침대다"가 설명 없이 읽힌다.**
 */
const LEDGE = {
  /**
   * 반폭은 이 사이에서 판마다 다르게 뽑는다.
   *
   * 같은 길이만 나오면 세 개가 서도 "같은 것이 세 번"이지 새로운 자리로 안 읽힌다.
   * 짧은 것은 한 칸짜리 기둥, 긴 것은 두어 개를 나란히 놓을 수 있는 자리가 된다.
   *
   * **아래끝은 `MAX_ITEM_HALF_WIDTH`(0.55)보다 커야 한다.** 좁으면 비행기·자전거가
   * 양쪽으로 삐져나와 놓자마자 굴러떨어진다 — 새 자리를 줬는데 쓸 수 있는 물건이
   * 따로 있는 셈이 된다.
   */
  minHalfWidth: 0.6,
  maxHalfWidth: 0.95,
  halfHeight: 0.14,
  /** 받침대 윗면에서 이만큼은 떠 있어야 한다. 붙으면 그 아래로 물건이 못 들어간다 */
  minClearance: 1,
  /** 한 판에 이만큼까지. 무제한이면 나중에 공중이 발판으로 즐비해진다 */
  maxCount: 3,
  /** 물건이나 다른 통나무와 이만큼은 떨어져야 한다 */
  margin: 0.18,
  /**
   * 히든 연출이 뭉쳐 통나무가 되기까지의 시간(초).
   *
   * **이 시간이 다 지난 뒤에 실제로 세운다.** 먼저 세워두고 연출만 얹으면 아직 보이지도
   * 않는 통나무가 물건을 받아내서, 허공에 걸린 것처럼 보인다.
   */
  formSec: 0.85,
} as const

/** 물건을 연달아 쏟아내 물리를 망가뜨리는 것만 막는 최소 간격 */
const DROP_COOLDOWN_MS = 300

/**
 * 히든을 가진 단어를 맞췄을 때 히든이 나올 확률.
 *
 * 이 값만으로는 체감을 알 수 없다. 실제로 보이는 빈도는
 * `히든을 가진 단어 수 / 전체 단어 수` 를 곱한 값이다. 물건을 20종에서 57종으로
 * 늘리면서 단어가 13개에서 50개가 됐는데 히든은 7개 그대로여서, 판당 기대 히든이
 * 1.5개에서 0.41개로 떨어졌다 — 대부분의 판에서 하나도 못 본다.
 *
 * 그래서 0.14에서 0.35까지 올렸는데, 그건 **종류가 적은 것을 빈도로 가린 것**이라
 * 같은 일곱 개가 자주 나오는 부작용이 따라왔다. 제대로 된 해법은 히든을 가진
 * 단어를 늘리는 것이고, 늘어난 만큼 이 값을 도로 낮춘다.
 *
 * | | 히든 보유 단어 | 확률 | 판당 기대 |
 * |---|---|---|---|
 * | 물건 20종 시절 | 7 / 13 | 0.14 | 1.5 |
 * | 57종 직후 | 7 / 50 | 0.14 | 0.39 |
 * | 빈도로 가린 것 | 7 / 50 | 0.35 | 0.98 |
 * | 문어·트리를 히든으로 | 9 / 48 | 0.26 | 0.98 |
 * | 지금 (합성 세트) | 11 / 78 | 0.34 | 0.96 |
 *
 * 판당 기대는 그대로 두고 종류만 늘린다 — 늘어난 것이 빈도로 나타나면 히든이
 * "가끔 나오는 다른 형태"이기를 그친다. `tests/ItemResolver.test.ts`가 이 기대치를
 * 위아래로 지켜본다.
 *
 * 합성 세트에서 값이 도로 올라간 것은 **재료 30종이 전부 단어이기 때문**이다. 분모가
 * 48에서 78로 뛰는 동안 히든은 둘밖에 안 늘었다. 다만 이번에는 히든이 유일한
 * "특별한 물건"이 아니다 — 합성 전용 18종이 생겼고 그쪽은 확률이 아니라 손으로 얻는다.
 * 그래서 여기를 더 밀어올리지 않는다.
 */
const HIDDEN_CHANCE = 0.34

/**
 * 판 앞머리(밭이 좁혀져 있는 동안)의 히든 확률.
 *
 * 앞머리 밭은 **같은 물건 둘짜리 레시피의 재료**만 내보내는데, 그 재료 7종은 전부
 * 히든 보유 단어다 — 같은 것 둘의 결과물이 곧 그 단어의 히든이니 구조적으로 그렇다.
 * 그래서 앞머리에서는 드롭마다 히든 확률이 걸린다. 평소에는 78단어 중 11개(14%)만
 * 히든을 갖고 있으니 밀도가 일곱 배로 뛴다.
 *
 * 실측(중앙 조준 봇 40판, 판당 18드롭 기준):
 *
 * | 앞머리 확률 | 첫 합성까지 | 앞머리 히든 | 판당 히든 |
 * |---|---|---|---|
 * | 0.34 (`HIDDEN_CHANCE` 그대로) | 8.8번 | **2.42** | **2.87** |
 * | 0.10 | 7.3번 | 0.80 | 1.31 |
 * | **0.05** | **6.6번** | 0.42 | **0.97** |
 * | 0 | 6.2번 | 0 | 0.57 |
 *
 * 앞머리 밭을 넣기 전 실측이 판당 0.75개였으므로 0.05가 그 자리로 돌려놓는 값이다.
 *
 * **전체 확률(`HIDDEN_CHANCE`)을 낮춰서 풀면 안 된다.** 원인이 확률이 아니라 앞머리
 * 밭의 구성이라, 전체를 건드리면 앞머리 뒤 구간까지 함께 마르고 그쪽은 이 프로젝트가
 * "히든이 너무 적다"에서 시작했던 자리다.
 *
 * 0으로 두지 않은 이유는 **앞머리에도 "무엇이 나올까"가 남아야** 하기 때문이다.
 * 다만 히든이 나오면 그것은 이미 결과물이라 재료가 하나 사라진다 — 그래서 이 값을
 * 낮추면 첫 합성도 함께 빨라진다.
 */
const OPENING_HIDDEN_CHANCE = 0.05

/** 안정화 판정: 이 속도 아래로 이만큼 유지되면 착지 완료로 본다 */
const SETTLE_SPEED = 0.35
const SETTLE_HOLD_SEC = 0.35

/** 물건이 받침대를 벗어날 때마다 하나씩 줄고, 0이 되면 끝이다 */
const LIVES = 3

/**
 * 목숨을 잃은 뒤 이만큼은 더 깎이지 않는다.
 *
 * 탑이 무너지면 물건이 하나씩 떨어지는 게 아니라 우수수 쏟아진다. 이탈을 각각 세면
 * 한 번의 무너짐으로 목숨 3개가 다 날아가서, 목숨이 3개인 의미가 사라진다.
 * 이 시간은 그 연쇄를 한 번의 실수로 묶어준다 — 쏟아지는 것이 끝날 만큼은 되고,
 * 다시 쌓기 시작할 때까지 남을 만큼 길지는 않은 값이다.
 */
const INVULNERABLE_SEC = 2

/**
 * 싱글에서 쓰는 주인 식별자.
 * 물리 층은 물건마다 주인을 들고 있어야 한다 — 멀티에서 물건이 벗어나면
 * 떨어뜨린 사람이 아니라 쌓은 사람의 목숨이 깎이기 때문이다. 싱글은 주인이 하나뿐이다.
 */
const SOLO_OWNER = 'solo'

const SCORE = {
  perItem: 100,
  perHeightMeter: 220,
  /** 콤보 1당 배수 증가분 */
  comboStep: 0.1,
  /** 배수 상한 */
  comboMaxMultiplier: 3,
  /**
   * 놓친 단어에 매기는 대가. 상쇄를 없앤 대신 점수로만 받는다.
   *
   * 최종 점수에 `floor + (1 - floor) x 정확도`를 곱한다. 정확도는 쌓은 개수를
   * 쌓은 개수와 놓친 개수의 합으로 나눈 값이다. 즉 많이 쌓을수록 실수 하나의
   * 무게가 옅어진다 — 놓쳤다고 판이 망가지면 안 되고, 다만 잘 친 판과는
   * 구분되어야 한다.
   */
  accuracyFloor: 0.4,
  /** 합성으로 만들어낸 물건에 얹는 점수 */
  craftBonus: 260,
} as const

const WORD = {
  /*
   * 좌우 각 칸 수. 여기 × 2가 화면에 동시에 뜰 수 있는 단어의 상한이다.
   *
   * 대전에서 사람이 늘면 단어를 더 내보내는데(Difficulty.forPlayers), 칸이 딱 맞으면
   * 자리를 못 찾아 조용히 걸러진다. 여유를 두어야 여덟이 붙어도 밭이 채워진다.
   */
  slotsPerSide: 5,
} as const

/**
 * 아레나를 화면에 그릴 때의 최대 폭(px).
 * 캔버스 자체는 낙하 레인 뒤까지 화면 전체에 깔린다 — 튕겨 날아간 물건과 히든 연출이
 * 받침대 영역을 벗어나도 보여야 하기 때문이다. 아레나(점선 틀)는 이 폭을 넘지 않으므로
 * GameScreen의 가운데 열과 렌더러가 같은 값을 본다.
 */
const ARENA_SCREEN_MAX_WIDTH = 570

/**
 * 이 질량 이상이면 "무거운 물건"이다. 착지하면 감쇠를 걸어 잠그고, 화면을 흔든다.
 *
 * **밀도가 아니라 질량으로 재는 이유**가 있다. 밀도로 재면 작고 조밀한 것(텀블러
 * 0.408)이 무겁다고 판정되고, 크고 실제로 더 무거운 것(비행기 0.410, 피자 한판
 * 0.526, 노트북 0.625)이 빠진다. 화면에서 무거워 보이는 것은 크기이므로,
 * 밀도로 재면 "가벼워 보이는 게 흔들고 무거워 보이는 건 조용한" 어긋남이 생긴다.
 *
 * 0.35에서 끊으면 도시락·노트북·피자 한판·접힌 노트북·비행기·텀블러가 들어오고,
 * 그다음(우산 0.183)까지 간격이 넓어서 경계가 애매하지 않다.
 */
const HEAVY_MASS = 0.35
const ANCHOR_LINEAR_DAMPING = 7
const ANCHOR_ANGULAR_DAMPING = 9

/**
 * 화면을 흔들려면 이만큼은 커 보여야 한다(그린 크기의 큰 변).
 *
 * 무게만 보면 눈과 어긋난다. 도시락은 **가장 무거운** 물건인데(질량 0.744)
 * 그림은 작은 편이고(0.75), 텀블러도 마찬가지다(질량 0.408, 크기 0.70).
 * 작은 것이 쿵 하고 화면을 흔들면 과장으로 읽힌다. 반대로 크기만 보면
 * 우산(0.98)이나 접힌 우산(0.80)처럼 가벼운 것까지 흔들게 된다.
 *
 * 0.78에서 끊으면 비행기·피자 한판·노트북·접힌 노트북이 남는다.
 * 도시락(0.75)과의 간격이 0.05로 좁으니, 새 물건을 넣을 때는 이 표를 다시 봐야 한다.
 */
const QUAKE_MIN_SIZE = 0.78

/** 무겁고 큰 물건이 이 속도 이상으로 부딪히면 화면이 흔들린다 */
const QUAKE_MIN_SPEED = 3.5

/**
 * 부딪힘을 **소리로 낼** 최소 속도.
 *
 * 지진(QUAKE_MIN_SPEED = 3.5)보다 훨씬 낮은 이유는 재는 대상이 다르기 때문이다.
 * 화면은 크고 무거운 것이 쿵 떨어질 때만 흔들려야 하지만, 소리는 가벼운 것이
 * 탑 위에 톡 얹히는 것까지 들려야 한다 — 얹혔는지 아닌지가 이 게임에서 가장
 * 자주 알아야 하는 사실이고, 눈은 다음 단어를 쫓느라 아레나에 없을 때가 많다.
 *
 * 더 낮추면 이미 쌓인 물건이 눌려 생기는 미세한 떨림까지 소리가 된다.
 */
const IMPACT_MIN_SPEED = 1.2

/**
 * 부딪힌 세기(속도 x 질량)를 0~1로 누르는 나눔값.
 *
 * 지진의 QUAKE_IMPACT_SCALE(4.5)과 따로 두는 이유는 재는 범위가 다르기 때문이다.
 * 지진은 가장 무거운 것만 보므로 위쪽에 맞춰져 있는데, 그 값으로 소리를 누르면
 * 대부분의 물건이 0.1 언저리에 몰려 전부 비슷하게 작다.
 *
 * 3.0은 가장 무거운 물건(도시락 0.744)이 제 속도로 떨어질 때 최대치를 넘고,
 * 가벼운 물건이 탑 위에 얹힐 때 0.1~0.3에 놓이는 값이다.
 */
const IMPACT_FULL_SCALE = 3
/**
 * 충격(속도 x 질량)을 0~1 세기로 누르는 나눔값.
 * 가장 무거운 물건이 제 속도로 떨어졌을 때 최대치에 닿고, 경계에 걸친 물건은
 * 눈에 겨우 보일 만큼만 흔들리는 값이다.
 */
const QUAKE_IMPACT_SCALE = 4.5
const QUAKE_DURATION = 0.45
/** 흔들림 최대 진폭 (월드 단위) */
const QUAKE_MAX_AMPLITUDE = 0.16
/**
 * 자리를 잡은 무거운 물건이 여기서 이만큼(월드 단위) 밀려나면 자리를 잃은 것으로 본다.
 * 그 순간 잠금이 풀리고 지진 판정도 되살아나서, 다시 부딪히면 또 흔들린다.
 * 얹힌 물건이 눌려 생기는 미세한 떨림으로는 넘지 못하고, 무너지기 시작할 때만 넘는 거리다.
 */
const QUAKE_REARM_DISTANCE = 0.4
/**
 * 자리를 잃은 물건이 다시 부딪힐 때는 이 속도만 넘어도 흔들린다.
 * 무너질 때의 낙하는 스택 한 칸 높이라 처음 떨어질 때만큼 빨라지지 않는데,
 * 그래도 무거운 것이 쏟아지는 순간이니 화면은 흔들려야 한다.
 */
const QUAKE_REIMPACT_SPEED = 2.2

export {
  ARENA,
  AIM_HALF_RANGE,
  CAMERA_HEADROOM,
  CAMERA_FOLLOW,
  MAX_ITEM_HALF_WIDTH,
  COUNTDOWN_SEC,
  SOLO_READY_MS,
  SOLO_START_MS,
  DIFFICULTY_FULL_HEIGHT,
  LEDGE,
  DROP_COOLDOWN_MS,
  HIDDEN_CHANCE,
  OPENING_HIDDEN_CHANCE,
  SETTLE_SPEED,
  SETTLE_HOLD_SEC,
  SCORE,
  LIVES,
  INVULNERABLE_SEC,
  SOLO_OWNER,
  WORD,
  ARENA_SCREEN_MAX_WIDTH,
  HEAVY_MASS,
  QUAKE_MIN_SIZE,
  QUAKE_IMPACT_SCALE,
  ANCHOR_LINEAR_DAMPING,
  ANCHOR_ANGULAR_DAMPING,
  QUAKE_MIN_SPEED,
  IMPACT_MIN_SPEED,
  IMPACT_FULL_SCALE,
  QUAKE_DURATION,
  QUAKE_MAX_AMPLITUDE,
  QUAKE_REARM_DISTANCE,
  QUAKE_REIMPACT_SPEED,
}
