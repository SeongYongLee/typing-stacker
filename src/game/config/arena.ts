/**
 * 아레나의 좌표계와 크기 — **판이 놓이는 자리**.
 *
 * 원점은 받침대 중앙, x는 오른쪽 +, y는 **위쪽 +**, 단위는 미터다. Rapier가 수 미터
 * 규모에서 가장 안정적이라 픽셀이 아니라 미터로 시뮬레이션하고 렌더러가 환산한다.
 */

/**
 * 아레나 좌표계: 원점은 받침대 중앙, x는 오른쪽 +, y는 위쪽 + (단위: 미터).
 * Rapier는 수 미터 규모에서 가장 안정적이라 픽셀이 아니라 미터로 시뮬레이션하고,
 * 렌더러가 px로 환산한다.
 */
const ARENA = {
  /*
   * 점선 틀의 반폭. 받침대 밖으로 이만큼 여유가 있다.
   *
   * 넓히면 굴러떨어지는 물건이 끝까지 보이고 조준할 여지도 넓어 보인다. 대신
   * 월드가 커지므로 같은 캔버스에서는 모든 것이 작아진다 — 그래서 아레나를 그리는
   * 화면 폭(ARENA_SCREEN_MAX_WIDTH)을 같은 비율로 함께 키워 받침대 크기를 지킨다.
   * 둘 중 하나만 바꾸면 물건이 커지거나 작아진다.
   */
  halfWidth: 3.2,
  height: 5.2,
  /** 받침대 윗면 높이 */
  platformTop: 0.8,
  /**
   * 받침대 반폭.
   * 양옆에 벽이 없으니 미끄러진 물건을 받아줄 것은 받침대 폭뿐이다.
   * 좁게 잡으면 한 칸 폭 탑을 쌓다가 살짝 미끄러지는 것만으로 즉사해서,
   * 실력과 무관하게 두세 개에서 끝난다.
   *
   * 2.0은 너무 관대해서 미끄러져도 대개 받침대가 받아냈다. 조준 범위가 이 값에서
   * 파생되므로(AIM_HALF_RANGE) 좁히면 조준도 같이 좁아진다 — 한 번에 크게 줄이지 않는다.
   */
  platformHalfWidth: 1.85,
  platformHalfHeight: 0.25,
  /**
   * 양끝의 **열린 덮개** — 받침대 밖으로 비스듬히 뻗은 판자다.
   *
   * 벽이 없는 판이라 미끄러진 물건은 그대로 굴러떨어졌다. 덮개는 그중 **살짝 밀린
   * 것만** 붙잡는다. 무너지는 것은 그대로 넘어가야 한다 — 막아 세우면 받침대가
   * 상자가 되어 이 게임의 유일한 실패 조건이 사라진다.
   *
   * **그림에서 잰 값이다.** `platform-front`의 왼쪽 끝을 열마다 훑으면 덮개가
   * 상자 벽과 갈라지는 자리(가로 6%)가 나오고, 거기서 바깥 끝까지가 이 판자다.
   * 바깥이 높고 안쪽이 낮아서, 밀려난 물건이 타고 **되돌아 들어온다.**
   *
   * 한때 이 자리에 네모난 턱을 세웠다. 그림의 기울기를 원근으로 오해해서
   * "옮기면 안 되는 것"으로 봤기 때문인데, 다시 재보니 실제로 뻗어 있는 판자였다.
   * 예전 값과 그때의 실측은 아래 표에 남긴다.
   *
   * **셋을 같은 조건에서 다시 쟀다**(물건 148종 · 18개씩 12판).
   *
   * | | 남은 물건 | 높이 |
   * |---|---|---|
   * | 없음 | 12.3개 | 1.60m |
   * | 네모 0.06m | 14.2개 | 1.98m |
   * | **덮개 (지금)** | **16.3개** | **2.23m** |
   *
   * 덮개가 네모보다 2.1개를 더 살린다 — 막는 것보다 되돌리는 것이 세다. 그래도
   * **18개 중 1.7개는 여전히 사라진다.** 실패가 없어진 것이 아니라 덜 잦아졌다.
   *
   * 아래는 물건이 107종이던 시절의 네모 값이다. 물건이 늘면 판이 통째로 달라지므로
   * 위 표와 직접 비교할 수 없고, 어디까지 시도해봤는지를 보려고 남긴다.
   *
   * | 턱 | 남은 물건 | 높이 |
   * |---|---|---|
   * | 없음 | 11.6개 | 1.84m |
   * | 0.04m | 13.1개 | 1.97m |
   * | 0.06m | 14.4개 | 2.25m |
   * | 0.12m | 14.7개 | 2.39m |
   * | 0.20m | 15.3개 | 2.39m |
   *
   * **조이고 싶으면 높이가 아니라 마찰이다.** 네모는 높일수록 세지지만 덮개는
   * 기울기와 마찰이 세기를 정한다. 지금 마찰 0.9는 물건과 같은 값을 그대로 쓴 것이라
   * 아무도 고른 적이 없다 — 낮추면 타고 오르다 미끄러져 그냥 넘어간다.
   */
  bowlFlap: {
    /** 바깥 끝. 받침대(±1.85) 밖으로 나간다 */
    outerX: 2.081,
    outerY: 0.99,
    /** 안쪽 끝 — 상자 벽과 갈라지는 자리 */
    innerX: 1.766,
    innerY: 0.871,
    /** 판자 두께. 그림에서 잰 세로 폭이다 */
    thickness: 0.026,
  },
  /**
   * 물건이 생성되는 높이. 카메라가 올라가면 이만큼 위에서 계속 떨어진다.
   * 탑이 이 높이에 닿으면 새 물건이 탑 속에 생겨 서로를 밀어내므로,
   * 카메라 없이는 여기가 곧 게임의 천장이었다.
   */
  spawnY: 4.6,
  /**
   * 이 높이보다 아래로 내려간 물건은 이탈로 본다.
   *
   * **받침대 아랫면이 0.3이므로 그 아래로 이만큼이 빈 칸이다.** 0.8일 때는 1.1월드가
   * 텅 빈 벽이었는데, 화면에서 그 자리는 받침대와 입력창 사이의 아무것도 없는 띠다 —
   * 미끄러진 물건이 지나가는 길일 뿐이라 넓을 이유가 없다.
   *
   * 0.25로 올려 0.55만 남긴다. 미끄러진 물건이 선을 넘는 장면은 여전히 보이고,
   * 판은 그만큼 아래로 내려앉아 위쪽에 벽이 더 열린다 — 카메라 여유를 늘린 대가를
   * 여기서 되찾는 셈이다.
   *
   * **받침대에 얹힌 물건이 여기 걸릴 일은 없다.** 가장 낮게 앉는 물건도 중심이
   * 받침대 윗면(0.8) 위에 있어서 한 뼘 이상 떨어져 있다.
   */
  killY: -0.25,
  /**
   * 지구 중력(-9.81)보다 약하게 잡았다.
   * 스폰 높이에서 받침대까지는 3m가 넘는데, 실제 중력이면 착지 속도가 8m/s를 넘어
   * 이미 쌓아둔 물건을 밀어내고 스택이 두세 개에서 무너진다.
   * 약한 중력은 낙하가 눈으로 읽히게 만들고 조준할 시간도 준다.
   */
  gravity: -7,
} as const

/**
 * 탑 꼭대기 위로 남겨두는 여유. 이보다 좁아지면 카메라가 따라 올라간다.
 *
 * 조준 화살표와 낙하 구간이 들어갈 만큼은 되어야 한다 — 붙자마자 닿을 만큼
 * 좁으면 무엇을 어디에 떨구는지 볼 시간이 없다.
 *
 * **이 값이 곧 탑 꼭대기의 화면 자리다.** 카메라가 따라가기 시작하면 꼭대기가
 * 화면 위에서 `여유 ÷ 월드 높이`만큼 내려온 곳에 고정된다 — 2.6이면 40%다.
 *
 * 2.6에서 3.4로 내린 것은 벽에 걸린 화이트보드 때문이다. 그것의 아래턱이 43.9%라
 * 탑 꼭대기(40%)와 정면으로 부딪혔고, 실제로 쌓아보니 조준 화살표가 보드 안에
 * 들어가 있었다. 3.4면 꼭대기가 52%로 내려와 보드 아래를 지난다.
 *
 * **보드를 올리는 쪽이 아니라 판을 내리는 쪽을 골랐다.** 보드에 곧 읽어야 하는
 * 글자가 적히기 때문이다 — 장식이라면 비켜 세우면 그만이지만, 정보라면 탑이
 * 그 앞을 지나는 것 자체가 문제다.
 *
 * 이후 3.0으로 조금 되돌렸다. 화이트보드 텍스트가 보드 안으로 흩어져 들어가며
 * 직접 충돌이 줄었고, 3.4는 박스가 너무 낮은 탑부터 내려가 게임 초반 카메라가
 * 조급하게 움직였다. 3.0이면 어느 정도 쌓인 뒤에야 카메라가 조금씩 따라간다.
 */
const CAMERA_HEADROOM = 3.0

/**
 * 카메라가 목표 높이를 따라가는 빠르기(초당 비율).
 * 즉시 옮기면 물건 하나 얹을 때마다 화면이 툭툭 튀어 어디에 떨어지는지 놓친다.
 */
const CAMERA_FOLLOW = 3.5

/**
 * 탑이 낮아졌을 때 카메라가 내려오는 빠르기.
 * 올라갈 때와 같은 속도면 작은 흔들림에도 판이 계속 내려와 고양이 타이밍이 불안정해진다.
 */
const CAMERA_DESCEND_FOLLOW = 1.2

/**
 * 판 시작 시 받침대를 미리 낮춰 보이게 하는 렌더 전용 오프셋.
 * 실제 물리 카메라와 스폰 높이는 건드리지 않는다.
 */
const CAMERA_START_VIEW_DROP = 0.28

/**
 * 카메라가 이만큼 실제로 올라가면 시작 오프셋은 사라진다.
 * 높은 탑 구간에서는 기존 카메라 구도를 그대로 쓰려는 것이다.
 */
const CAMERA_START_VIEW_DROP_FADE_HEIGHT = 1.0

/** 가장 큰 물건(비행기)의 반폭. tests/shapes.test.ts가 이 값을 지킨다 */
const MAX_ITEM_HALF_WIDTH = 0.55

/**
 * 화살표가 훑는 범위. 받침대 반폭과 같다.
 *
 * 예전에는 가장 큰 물건의 반폭을 빼서, 어느 타이밍에 Enter를 쳐도 물건이 받침대에
 * 온전히 얹히게 했다. 조준 실수로 즉사하지 않게 하려는 안전장치였는데 대가가 있었다 —
 * 조준이 틀려도 손해가 없으니 이 게임의 핵심 긴장("빨리" 대 "정확히")에서 정확히
 * 쪽의 보상이 사라졌다. 실제로 봇으로 일부러 가장자리만 노려도 판이 끝나지 않았다.
 *
 * 이제 받침대 끝까지 조준할 수 있다. 끝을 노리면 물건이 절반쯤 걸치므로 Enter를
 * 언제 누르는지가 곧바로 값을 치른다.
 */
/**
 * 받침대 밖으로 조준할 수 있는 여유.
 *
 * 끝을 노리면 물건 중심이 받침대를 벗어나므로, 작은 물건은 얹히지 못하고 그대로
 * 떨어진다. 조준이 실제로 값을 치르게 하려는 것이다 — 받침대 안에서만 조준하면
 * 어디를 노리든 일단 얹히고, "정확히"의 대가가 늦게 흐릿하게만 돌아온다.
 */
const AIM_OVERHANG = 0.25

const AIM_HALF_RANGE = ARENA.platformHalfWidth + AIM_OVERHANG

/**
 * 아레나를 화면에 그릴 때의 최대 폭(px).
 * 캔버스 자체는 낙하 레인 뒤까지 화면 전체에 깔린다 — 튕겨 날아간 물건과 히든 연출이
 * 받침대 영역을 벗어나도 보여야 하기 때문이다. 아레나(점선 틀)는 이 폭을 넘지 않으므로
 * GameScreen의 가운데 열과 렌더러가 같은 값을 본다.
 */
const ARENA_SCREEN_MAX_WIDTH = 570

/**
 * 이보다 좁으면 판을 열지 않고 안내만 띄운다(px).
 *
 * **레이아웃이 넘치는 하한은 676이다** — 판 화면이 `레인 172 + 아레나 260 + 레인 172`에
 * 간격과 여백을 더한 값이고, 172는 가장 긴 단어(크리스마스트리, 153px)가 잘리지 않는
 * 폭이며 260은 아레나가 더 못 줄어드는 폭이다.
 *
 * **그런데 "안 깨진다"와 "할 만하다"는 다르다.** 아레나는 받침대(3.7m)가 아니라
 * 튕겨 나간 물건까지 보이는 **6.4m**를 그리므로, 화면 폭이 곧 물건 크기다.
 *
 * | 뷰포트 | 아레나 | 1m당 | 가장 작은 물건(0.4m) |
 * |---|---|---|---|
 * | **1200 이상** | **448px** | **70px** | **28px** |
 *
 * 448에서 멈추는 것은 `ARENA_SCREEN_MAX_WIDTH`(570) 때문이 아니라 판 화면의
 * `maxWidth: 1200`과 레인 최대폭(340) 때문이다 — **그 상수는 도달하지 않는다.**
 * | 1024 | 272px | 43px | 17px |
 * | 900 이하 | 260px | 41px | 16px |
 *
 * **1200을 고른 것은 그 위로 더 커지지 않기 때문이다.** 판 화면에 `maxWidth: 1200`이
 * 있어서 1200이 곧 설계된 크기이고, 여기가 **타협이 시작되지 않는 가장 좁은 폭**이다.
 *
 * 1024를 받으면 물건이 28 → 17px로 **40% 작아진다.** 스티커 아트의 검은 윤곽 안쪽이
 * 뭉개지고, 부스러기(월드 0.055~0.115)는 2~5px이 되어 눈에서 사라진다.
 *
 * **1024를 받으려면 그리드를 손봐야 한다.** 레인 최대폭 340을 280으로 줄이면 1024에서
 * 아레나가 392px(1m당 61px)이 되어 1200에 가까워진다 — 단어가 잘리지 않는 172까지는
 * 여유가 있다. 그 대가는 단어 두 줄이 좁아지는 것이고, 지금은 그쪽을 지켰다.
 *
 * 세로는 재지 않는다. 이 게임은 좌우로 벌어진 레인이 구조를 정하므로 좁아지는 쪽이
 * 늘 가로다 — 세로가 모자라면 아레나가 알아서 작아지고 그것은 깨지는 것이 아니다.
 */
const MIN_VIEWPORT_WIDTH = 1200

export {
  ARENA,
  CAMERA_HEADROOM,
  CAMERA_FOLLOW,
  CAMERA_DESCEND_FOLLOW,
  CAMERA_START_VIEW_DROP,
  CAMERA_START_VIEW_DROP_FADE_HEIGHT,
  MAX_ITEM_HALF_WIDTH,
  AIM_OVERHANG,
  AIM_HALF_RANGE,
  ARENA_SCREEN_MAX_WIDTH,
  MIN_VIEWPORT_WIDTH,
}
