import type { DifficultyLevel } from '../types/game.ts'

/**
 * 난이도는 **고정**이다. 시간이 지나도 오르지 않는다.
 *
 * 예전에는 12초마다 한 단계씩 5단계까지 올랐다. 없앤 이유는 압박의 축이 겹쳤기
 * 때문이다 — 이 게임은 스택이 높아질수록 저절로 어려워진다(무게중심이 높아지고
 * 얹을 자리가 좁아진다). 거기에 시간 압박을 더하면 두 축이 같은 방향으로 겹쳐서,
 * 실력이 늘어도 판이 길어지지 않고 "언제 끝나는지"만 시간이 정해버린다.
 *
 * 고정값은 예전 1단계 값이다. 낙하 6.5초는 단어 평균 6.3타(사람 속도로 1.1~1.5초)의
 * 약 4배 여유이고, 화살표 왕복은 4.2초(= 2 / aimSpeed)다.
 *
 * 동시 낙하 상한은 단어 풀(13개)과 레인 칸 수(4×2)에 묶인다. 활성 단어의 중복을
 * 막으므로 상한이 풀 크기에 가까우면 화면이 거의 전체 어휘로 채워져 스폰이 막힌다.
 */
const DIFFICULTY: DifficultyLevel = {
  spawnInterval: 1.6,
  fallDuration: 6.5,
  aimSpeed: 0.48,
  maxConcurrent: 5,
}

export { DIFFICULTY }
