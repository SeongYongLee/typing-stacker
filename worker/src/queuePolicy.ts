/**
 * 이만큼 물어보지 않으면 줄에서 치운다.
 *
 * 브라우저는 창을 닫을 때 알려주지 않으므로 **멎은 것으로만 사라진 것을 안다.**
 * 물어보는 주기(1.5초)의 몇 배로 둔다 — 짧으면 잠깐 끊긴 사람이 줄에서 빠지고,
 * 길면 이미 없는 사람과 짝이 맺어져 아무도 오지 않는 방이 열린다.
 */
export const QUEUE_STALE_MS = 6000
