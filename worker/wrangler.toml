name = "typing-stacker-relay"
main = "src/index.ts"
compatibility_date = "2026-08-01"

# 방 하나가 Durable Object 하나다. 같은 방 코드는 언제나 같은 인스턴스로 간다 —
# 그래서 방장과 참가자가 서로를 찾는 데 별도의 조율이 필요 없다.
[[durable_objects.bindings]]
name = "ROOMS"
class_name = "MatchRoom"

# SQLite 백엔드여야 무료 플랜에서 쓸 수 있다 (KV 백엔드 DO는 유료 전용이다)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["MatchRoom"]
